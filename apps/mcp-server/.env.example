# CustomerEQ MCP Server Configuration
# Copy to .env and fill in values

# Base URL of the CustomerEQ API
CUSTOMEREQ_API_URL=http://localhost:4000

# --- Option A: Production (Clerk JWT) ---
# CUSTOMEREQ_API_TOKEN=your-clerk-jwt-token

# --- Option B: Local dev (test mode, no JWT needed) ---
# Set this to your brand ID (find it in the DB or use the one from seed data)
CUSTOMEREQ_TEST_BRAND_ID=cmn689ibu000089tqad1g234t
