# CustomerEQ MCP Server Configuration
# Copy to .env and fill in values

# Base URL of the CustomerEQ API
CUSTOMEREQ_API_URL=http://localhost:4000

# API authentication token (Clerk JWT or test token)
# For local dev: use X-Test-Brand-Id header instead (set in the API's .env NODE_ENV=test)
CUSTOMEREQ_API_TOKEN=
