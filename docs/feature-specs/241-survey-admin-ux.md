# Feature: Survey Admin UX — green-slate revamp

Issue: [#241](https://github.com/mathursrus/CustomerEQ/issues/241)
Owner: manohar.madhira@outlook.com
Status: **Ready for review (round 11 — OQ4 resolved with JS-prefill design per D51; all open questions now closed; awaiting reviewer signoff).**
Last touched: 2026-05-11

---

## Customer

The primary persona is the **marketing manager** at a mid-market brand on CustomerEQ. They own the survey end-to-end — they create it, configure its distribution, set the thank-you copy, and (once Post-Survey Actions ship in a future iteration) configure the rules that fire on response. They are not necessarily a developer. Their mental model is "I want to measure NPS after a tier upgrade" or "I want a one-question feedback widget on my product page" — not "I want to configure a Trigger then a SurveyBuilder then an EarningRule."

A secondary persona is the **brand admin** who configures brand-level defaults — consent mode, member-identifier kind, brand-theme library — in [Organization Settings (#277)](277-organization-settings.md). The survey editor inherits from those defaults and exposes per-survey override only where business value warrants. In small organizations the two personas may be the same person; the surfaces are designed so the marketing manager never needs to enter Organization Settings to ship a survey. RBAC implication: the survey editor's theme picker is a *selector* over the brand theme library; theme creation/editing remains in Organization Settings because survey creators may not have theme-edit access.

A tertiary persona is the **respondent** — the customer who sees and submits the survey, either via a CustomerEQ-hosted link or rendered inline by the brand's widget JS on their own page. #241 owns how the brand admin *configures* what the respondent sees; #231 owns how responses are persisted.

## Customer's Desired Outcome

1. **One coherent surface** for creating, editing, and managing a survey from idea to live data — no choice between a wizard and a builder; no broken-link edit path; no accidental duplicate drafts.
2. **Compliant by default, customizable when justified.** Consent inherits from the brand. The survey owner overrides only when they have a reason; a more-permissive override requires an attestation that names who authorized it, when, and why — the same audit shape the brand-side editor uses.
3. **Predictable points behavior.** A single configured value credits exactly once per accepted response through the program, atomically with a `LoyaltyEvent` row. Points appear in the invitation and in the thank-you message — never on the survey form itself, where their presence depresses response quality.
4. **One survey, many channels.** A single survey row is reusable across share link, embed widget, email integration, QR — every channel live simultaneously, no per-channel duplication. The form-renderer adapts to the channel; per-channel chrome (logo / brand name / title visibility) is configurable in one place.
5. **Detail page that matches the job at hand.** Before responses land, the page focuses on edit + distribute. After responses land, configuration recedes and analytics takes the foreground — without the operator switching pages or templates.

## Customer Problem being solved

Today's survey admin experience is *visibly* fragmented. The 2026-05-10 directive on #241 enumerates the cluster:

- **Three entry points, no clear primary.** `/admin/surveys/new` (adhoc form-based), the triggered wizard (4 steps with rule-builder), and `/admin/survey-builder` (drag-drop question canvas). Each writes to the same `Survey` row but exposes a different subset of fields — a survey created in one path is not editable in another without code-reading.
- **No way to view a survey or edit a draft.** The list page lacks Edit affordances; the detail page has no Edit button (admins guess the `/edit` URL); the `/edit` URL is a redirect stub to survey-builder which only edits questions — not status, theme, consent, or thank-you.
- **Duplicate drafts.** The wizard POSTs `/v1/surveys` on every "Next" click — navigating back-and-forward produces orphan rows.
- **Vocabulary drift.** "Launch", "Activate", "Publish" used inconsistently for the same DRAFT→ACTIVE transition. "Close" used for an irreversible-feeling action that's actually reversible. Builder's "Launch" leaves the survey in DRAFT, requiring a separate Activate.
- **Two parallel earning paths.** `Survey.incentivePoints` (decorative widget badge) plus `EarningRule.triggerEvent='survey_completion'` (separate wizard, mismatched vocabulary). Empirically confirmed prod bug: customer configures the EarningRule, member submits NPS, `pointsBalance` stays at zero.
- **Rule-action picker contract broken.** The current dropdown lists six action types; five save with `actionConfig: {}` and the worker silently drops them.
- **Consent override missing from the editor.** #276 shipped the schema columns and the data migration to unblock production; #283 carries the PATCH endpoint and the editor panel. Both are #241's home — `#283` is absorbed into this Epic.
- **No surface for survey-theme UI elements.** Brand theme is now separated from survey theme (#291), but the survey owner has no way to pick a theme or set a thank-you message in the editor.
- **Embedded vs. standalone differences are configured nowhere.** Today the difference is post-launch only — a "Copy Widget" button on the detail page if status=ACTIVE. The owner can't preview an embedded form or tune brand-chrome visibility per channel.
- **GDPR / CCPA paths exist in schema but not in editor.** Brand-level defaults from #277/#292 are shipped; the survey-level override surface is not.

The cumulative effect: a marketing manager who wants to "ship a feedback survey on my product page" has to learn three editors, two earning models, and one broken picker — and is one unconfirmed click away from creating a duplicate draft or stopping a live survey. The directive is to redesign this end-to-end rather than patch field-by-field.

## User Experience that will solve the problem

The mock at [`mocks/241-survey-admin-ux.html`](mocks/241-survey-admin-ux.html) is the working artifact (fully interactive — reviewers walk the flow in-browser). Sections below describe the experience at the level of "specific steps the user takes"; the mock is the source of truth for visual layout.

### §1. Surveys list

- **Path**: `/admin/surveys`
- **One primary CTA**: `+ New survey` creates a draft and routes to the editor (`/admin/surveys/[id]/edit`).
- **Columns** (left-to-right): **Name** (with description + program in a muted meta line below) · **Type** (its own column — NPS / CSAT / CES / Custom rendered as a discrete pill) · **Status** (badge) · **Responses** · **Updated** (relative time) · row actions. Type-as-column prepares for column-header sortability later.
- **Row click** opens the read-only Detail page (Programs / Campaigns pattern). Edit happens from there, or directly via the row-end ✎ button.
- **Row actions**: Edit (✎) + More (⋯).
- **⋯ menu (state-aware)**: Duplicate (always) · Discard draft (DRAFT only) · Pause (ACTIVE only) · Stop (ACTIVE / PAUSED only) · Restart (STOPPED only) · Delete (STOPPED only, with confirm).
- **Filter chips** at the top: **Status** (All / Draft / Active / Stopped) and **Type** (NPS / CSAT / CES / Custom). No Trigger or Distribution chips — CustomerEQ doesn't natively trigger surveys in V0, and distribution is multi-channel (not a categorical attribute).
- **Status badges** use the converged vocabulary: Draft / Active / Paused / Stopped. (Schema rename `SurveyStatus.CLOSED → STOPPED`.)

### §2. The Survey editor — 4 horizontal tabs

- **Path**: `/admin/surveys/[id]/edit`.
- **Shape**: horizontal tabs at the top of the page, one tab body visible at a time. Tabs in order: **Basics → Questions → Look & Feel → Points & Thank You**. Each tab body ends with **Back** and **Continue** buttons; the auto-save indicator sits in the page header alongside the status pill, and the primary `Activate` button sits top-right.
- **Saving**: each field auto-saves on blur (debounced) into the same `Survey` row that was created at "+ New survey" time. No POST on tab navigation. Activation calls `PATCH /v1/surveys/:id/status DRAFT → ACTIVE` and gates on (a) `Survey.questions.length >= 1` and (b) all required fields complete.

#### §2.1 Basics

The foundation tab. Everything the form needs to render and persist responses.

- **Internal name** (`Survey.name`) — admin-only label. Required. "Used in the surveys list and analytics. Only your team sees this."
- **Survey title** (`Survey.title`, **new nullable column**) — respondent-facing form heading. Required. Subject to per-channel visibility toggle in Look & Feel. Existing surveys are backfilled at migration time to `Survey.name`.
- **Type** (NPS / CSAT / CES / Custom) — a 4-card grid with one-line "use this when" captions on each card. Picking NPS / CSAT / CES auto-populates the Questions tab with the standard set (all editable + reorderable). Custom = blank canvas.
- **"Not sure which to pick?"** — collapsed by default; expands to a guidance block (loyalty checkpoint → NPS, post-experience → CSAT, post-effort → CES, no-fit → Custom). The toggle uses the platform's standard `▼` chevron (rotated -90deg when collapsed) per the Organization Settings convention.
- **Program** — required FK; points credit through this program.
- **Description** — optional admin note; surfaces under the survey name in the list.
- **Response policy** (per #231) — single dropdown with three options: Allow multiple responses (default) / One response per member / Latest overwrites prior.
- **Consent collection** (sub-block) — see §2.1.1.

##### §2.1.1 Consent collection sub-block (inside Basics)

A single dropdown for consent mode with a live preview below and an optional disclosure-text override at the bottom — the same shape the Organization Settings consent surface uses.

- **Consent mode dropdown**:
  - `Inherit brand default · <resolved mode>` (default; e.g., "Inherit brand default · Explicit consent required")
  - `Override · Implied on submit` (visible only if it differs from brand default — per #276 round-1 reviewer note carried into #283)
  - `Override · Explicit consent required` (visible only if brand default is Implied)
- **Override-to-more-permissive triggers attestation.** Setting the dropdown to a more-permissive value surfaces an amber callout in the section ("This deviation will be logged — when you save, you'll be asked to confirm and supply a reason"). On Save, the **attestation modal** fires: identity (auto-filled), reason text (required, ≤500 chars, soft-warns on PII shapes), checkbox attestation. On confirm, `Survey.consentMode`, `Survey.consentSuppressedAttestedBy`, `Survey.consentSuppressedAttestedAt`, and `Survey.consentReason` are written via the PATCH endpoint absorbed from #283.
- **Override-to-stricter** does not trigger attestation (it's a more conservative posture) but still writes an audit row.
- **Preview card** — directly below the dropdown. Renders the disclosure exactly as the respondent will see it: with checkbox when Explicit; without checkbox (paragraph form) when Implied; **empty (no consent block rendered at all) when the disclosure text is blank**. Mode indicator in the preview header ("Explicit · with checkbox" / "Implied · no checkbox" / "No consent block") updates as the dropdown and the textarea change.
- **Disclosure text override** (`Survey.consentTextOverride`) — uses **the same editor UX as the Organization Settings disclosure text editor**: a `.consent-toolbar` above the textarea with two insert buttons (`[+] Privacy link`, `[+] Terms link`) and a `Reset to brand default` link. Clicking a button inserts a token of the form `{{privacy:"Privacy Policy"}}` or `{{terms:"Terms and Conditions"}}` at the cursor (wraps the current selection if any). Tokens resolve at render time to `<a>` tags pointing at `Brand.privacyPolicyUrl` / `Brand.termsUrl`. If the brand has no Privacy URL, the same field-warning #277 surfaces appears here. **The Terms link button is rendered only when `Brand.termsUrl IS NOT NULL`** — hidden when the brand has no Terms URL (matches the Organization Settings disclosure editor's behavior). The preview updates live as the textarea changes.
- **Blank disclosure is a valid setting**, not an error. When the survey owner clears the textarea (and clears the inherit fallback), the respondent sees no consent block at all. This is the right shape for surveys whose respondents are outside GDPR/CCPA scope (e.g., respondents in Washington state, B2B internal employees). The survey owner bears the legal authorization burden for blanking the disclosure; the audit log records the change with actor + timestamp + new value so the deviation is traceable post-hoc.
- **Audit-trail badge** rendered on the survey detail page header when `Survey.consentMode != null` OR `Survey.consentTextOverride` is empty: shows attester + timestamp + reason snippet (for mode-override) or the blank-disclosure attribution (for empty-text).

#### §2.2 Questions

The existing `/admin/survey-builder` UX, embedded here as the Questions tab. Drag-drop canvas + question-type palette (11 types per #35) + per-question config panel on the right.

When the Type field in Basics is NPS / CSAT / CES, the canvas pre-populates with that type's standard question set — all editable, all reorderable, deletable. A muted preset banner at the top of the canvas names the active preset; switching the type in Basics updates the preset banner here. Custom = blank canvas, palette is the only source of new questions.

Selecting a question reveals its config in the right rail: text, type, required, skip logic, branching. Same shape as today's builder.

#### §2.3 Look & Feel

Preview-first. The respondent's-eye view is the focus of the tab; theme picker and chrome matrix are the means.

- **Live preview — channel-first.** Top tabs: `🔗 Standalone (link)` and `🧩 Embedded (widget)`. Within each tab, **Desktop preview and Mobile preview render side-by-side**. Each preview shows the actual configured theme, the chrome matrix applied to its channel, the question set, the consent disclosure (if any), the **member-identification field** (per the channel-specific rules below), and the submit button.
- **Member-identification (per-channel behavior — V0)**:
  - **Standalone surveys** — the form **renders a member-identification field** based on the brand's `memberIdentifierKind` setting from Organization Settings (#277 — `email` / `phone` / `external_id` / `prompt`). The respondent must provide it to submit. This is reflected in the Standalone preview so the survey owner sees the same flow respondents will.
  - **Embedded surveys** — the host page passes member identity to the widget **out of the URL** (per D51 / OQ4 resolution). Two supported integration patterns: (1) **data-attribute prefill** — host page server-renders `<script src="…/widget.js" data-survey="srv_abc" data-prefill-email="{{user.email}}" data-prefill-external-id="{{user.id}}"></script>`; one-paste ergonomic, no URL surface. (2) **JS prefill API** — `CustomerEQ.surveys.prefill('srv_abc', { email, externalId, firstName })` after widget mount; for SPA contexts where identity is known mid-session. The Embedded preview renders a small "Member identity prefilled by host page" placeholder where the field would otherwise appear. **Fallback**: at runtime, if neither prefill mechanism provides identity, the form falls back to rendering the same identification prompt the Standalone surveys use — the survey still works; it just degrades to asking the respondent directly. **Auto-enroll (#231)** runs unchanged in all cases because the identifier reaches the server via POST body (`resolveOrEnrollMember` consumes POST fields), not via URL.
- **Theme picker** — card grid over the brand's BrandTheme library. Each card shows a primary-color swatch + font sample. The brand's default theme is marked and pre-selected. **Shows ALL brand themes** — no count cap. The survey editor is a theme *selector*, not an editor; no `Manage themes →` link surfaces here because the survey creator may not have theme-edit RBAC. Theme creation / editing lives in Organization Settings. CustomerEQ provisions 4 default themes per brand at brand creation; brand admins can add more.
- **Per-channel chrome matrix** — a small table with three rows (Brand logo / Brand name / Survey title) and two columns (Standalone / Embedded). Each cell is a toggle. Defaults: full chrome on Standalone (everything on); minimal on Embedded (only Survey title on — the host page already shows the brand). The form-renderer reads the matrix at render time. Toggles apply to both Desktop and Mobile previews of the active channel simultaneously.

#### §2.4 Points & Thank You

Single home for the post-submit experience. Points configuration **lives on the program**, not on the survey — this section displays the resolved value and lets the survey owner write the thank-you copy.

- **Points awarded for completion** — **read-only display** sourced from the survey's associated program (per D50 OQ5 resolution). Renders as an inherit-row showing "*N* `<pointCurrencyName>` per accepted *<type>* response — set in *<programName>* program" with a link to the program (subject to RBAC; opens read-only if the survey owner lacks edit access). The value is the program's `EarningRule(triggerEvent=cxEventForType(Survey.type), pointsAwarded=N)` — where `cxEventForType` maps NPS→`cx.nps_response`, CSAT→`cx.csat_response`, CES→`cx.ces_response`, Custom→`cx.survey_completed`. **Per-survey-type points are natural**: a program can award different points for each survey type by configuring one EarningRule per cx event. If the program has no matching EarningRule for the survey's type, the row reads "No points configured for *<type>* survey completion" with a pointer for the program admin to set one up.
- **Thank-you message** — textarea with insert-variable picker. **V0 variables**: `{{points}}` (the points credited for this response — program base rate × any active campaign multipliers), `{{pointCurrencyName}}` (e.g., "Stars"), `{{rewardLink}}` (URL to the member's rewards page). **Not in V0**: `{{programName}}` (internal name not for respondent display) and `{{memberName}}` (name is not guaranteed to be on the Member record — at best the platform has `memberId`, which isn't shown to the respondent). Default copy: "Thank you for your feedback! Your `{{points}}` `{{pointCurrencyName}}` are on their way to your account."
- **Thank-you redirect URL** (standalone only) — optional URL. If set, post-submit redirects after 3 seconds. Embedded surveys ignore this and show the in-place thank-you state. (Embedded contexts don't navigate the host page.)
- **No in-form points badge. No `showIncentivePoints` toggle.** Points are surfaced in the invitation (email subject, embed snippet preview metadata, share-link unfurl description) and in the thank-you message via `{{points}}` — never on the survey form itself. This protects response quality (Singer & Ye 2013) while still rewarding the member.
- **V1 hook**: per-survey override of the program's base earn rate (a nullable `Survey.pointsOverride` column). The survey editor would then show an editable override field next to the inherited value. Deferred per D40 — V0 keeps the earning configuration entirely on the program.

### §3. One survey, many channels

A single survey row is reusable across every distribution mechanism. The owner does **not** pick "Standalone" or "Embedded" at draft time — they author once and the survey is simultaneously available as:

- **Share link** — CustomerEQ-hosted at `/s/<surveyId>`.
- **Embed widget** — host-mounted JS snippet rendering the same form inside the brand's own page.
- **Email integration** — pre-rendered into outbound campaign emails.
- **QR code** — generated on demand for print, signage, or in-store kiosks.
- **Future channels** — added as the platform grows; survey rows don't need to know about them in advance.

The **form-renderer** adapts to the channel context, per the Look & Feel chrome matrix. The detail page surfaces every distribution mechanism side-by-side post-activation (§7).

### §4. Single earning path (program-owned — V0)

Programs own earning; surveys consume earning. The two parallel paths that exist today consolidate to one — and the chosen direction matches the loyalty-domain RBAC split (program admins configure earning rules; survey owners configure surveys).

- **Path A (today, dropped)**: `Survey.incentivePoints` — a per-survey integer that decoratively rendered as a widget badge. Never actually credited to `pointsBalance` (the root of the #225 prod bug). **Removed in V0.**
- **Path B (today, kept and fixed under D50)**: `EarningRule(programId, triggerEvent='cx.<surveytype>_response', pointsAwarded=N)` defined on the program. The canonical loyalty primitive — same shape used for other earn triggers (`purchase`, `enrollment`, etc.); the cx-event trigger string just slots in. **Kept in V0; bug fixed; per-survey-type configuration naturally supported via one EarningRule per cx event.**

**Editor display** (per §2.4): the Points & Thank You section's "Points awarded for completion" row READS from `EarningRule(programId = Survey.programId, triggerEvent = cxEventForType(Survey.type))`. Read-only at V0; the survey owner sees what their respondents will earn for *this specific survey type* but cannot edit the value from the survey editor (the value belongs to the program).

**Backend pipeline** (per OQ5 resolution → D50): on accepted-response submit, the response handler emits **exactly one event** whose `eventType` is the cx event for the survey's type — `cx.nps_response` / `cx.csat_response` / `cx.ces_response` / `cx.survey_completed`. The existing EarningRule evaluator (the worker plug that handles other trigger events) consumes the event, resolves `EarningRule(programId = Survey.programId, triggerEvent = <emitted-cx-event>)`, and writes a `LoyaltyEvent` (action `EARN`) atomically with the `pointsBalance` update. The exact-string-match contract of the evaluator (verified at `apps/worker/src/processors/loyaltyEvents.ts:43-105`) needs no change — `EarningRule.triggerEvent` is already a free-text field. **Same event feeds the SurveyRule → Campaign trigger pipeline (#80)** for score-range matching; no event duplication. **Campaign multipliers** are NOT layered onto the EarningRule's base rate in V0 — see **OQ3** for the architectural decision deferred to the RFC.

**Per-survey-type points configuration is now naturally supported**: a single program can award different points for each survey type by creating one EarningRule per cx event (e.g., 50 Stars for `cx.nps_response`, 20 Stars for `cx.csat_response`, 30 Stars for `cx.ces_response`, 25 Stars for `cx.survey_completed`).

**Bug fix** (revised under D50): the #225 prod bug ("customer creates EarningRule for survey_completion, member submits NPS, pointsBalance stays 0, zero LoyaltyEvent rows") is fixed by **education + migration, not by adding a new event**. The empirical bug was that admins configured `EarningRule(triggerEvent='survey_completion')` expecting it to fire on survey responses — but `survey_completion` was never an emitted event type. Under D50: (a) the EarningRule wizard surfaces the canonical cx event triggers (`cx.nps_response` / `cx.csat_response` / `cx.ces_response` / `cx.survey_completed`) as picker options, so future admins configure correctly; (b) the migration fan-out (next bullet) auto-converts existing `EarningRule(triggerEvent='survey_completion')` dead rows to live per-type rules, preserving operator intent.

**Migration plan** (revised under D50):
- **Drop `Survey.incentivePoints` column.** (See V1 note below if it gets repurposed for per-survey override.)
- **Auto-create per-type EarningRules** for brands with prior intent on Survey.incentivePoints: for each `(programId, cxEventForType(Survey.type))` pair where at least one survey in the program has `incentivePoints > 0`, create `EarningRule(programId, triggerEvent=cxEventForType(...), pointsAwarded=mode(survey.incentivePoints for surveys of that type in that program), attribution='__migration_241__')`. Mode chosen over max to reflect typical operator intent; RFC may adjust.
- **Existing dead `EarningRule(triggerEvent='survey_completion')` rows** (the rules that never fired, per #225 / D50 finding): **fan out** to one rule per cx event type the program's surveys actually use. The dead rule is then deleted. This preserves the operator's "earn points on any survey completion" intent across all survey types in that program. Same `__migration_241__` attribution applied to the new rules.
- **For brands with explicit `EarningRule(triggerEvent='cx.*_response')` already**: untouched — the operator has already moved to the canonical shape.
- **Affected brands** surfaced via a one-line notice in Organization Settings (#277): "Earning consolidation: survey-completion earning is now per-survey-type. Your program rules have been fanned out to cx.nps_response / cx.csat_response / cx.ces_response / cx.survey_completed where they applied. Review your earning rules → [Programs]." Includes a count of surveys affected + new EarningRule count.
- **#315 (filed earlier as 'response handler doesn't emit survey_completion event') is now CLOSEABLE** under this design — there is no separate `survey_completion` event to wire. The "bug fix" reduces to "ensure brand admins configure EarningRule with the cx event triggers, not the non-existent survey_completion trigger". The fan-out migration above handles existing dead rules automatically. Per-thread reply on #315 will note this.

**V1 hook (deferred)**: per-survey override of the program's base earn rate, as a nullable `Survey.pointsOverride` column. When set on a survey, the worker resolves to `survey.pointsOverride ?? earningRule.pointsAwarded`. Filed when demand justifies it. The Points & Thank You section retains layout room for the future override field next to the inherited program value.

### §5. Vocabulary

| Operation | UI text | Status transition |
|---|---|---|
| Save without status change | **Save Draft** (auto-save indicator: "Saved · Xs ago") | n/a |
| Make survey live | **Activate** (gates on ≥1 question) | DRAFT → ACTIVE |
| Halt a live survey | **Stop** (modal-confirmed; reversible) | ACTIVE → STOPPED |
| Re-enable a stopped survey | **Restart** (modal-confirmed) | STOPPED → ACTIVE |
| Discard before activation | **Discard draft** (modal-confirmed) | DRAFT → (deleted) |
| Pause for time-bounded edits | **Pause** (rare — for changing questions on a live survey without losing accumulated responses) | ACTIVE → PAUSED |
| Resume from pause | **Resume** | PAUSED → ACTIVE |

- "Launch" is removed from all UI strings.
- "Close" is removed; replaced with "Stop".
- Schema rename: `SurveyStatus.CLOSED → STOPPED`. One migration; UI updates lockstep.

**All survey state transitions are audit-logged.** Every transition in the table above (Activate / Pause / Resume / Stop / Restart / Discard / Delete) writes an audit-log entry capturing `{ actorUserId, surveyId, fromStatus, toStatus, timestamp }` via the existing audit plugin (`apps/api/src/plugins/audit.ts`). The audit log feeds compliance reporting and surfaces in any future "survey activity" view (out of scope for #241; the data substrate is in place).

### §6. Activate flow

When the owner clicks `[Activate]` from the editor (top-right header or `Continue` on the Points & Thank You tab), a confirmation modal appears with:

- A short summary panel — type, response policy, consent mode (with override-attribution if applicable), points awarded, theme.
- Cancel returns to the editor.
- **Activate & go to detail** confirms and redirects to the survey's Detail page (`/admin/surveys/[id]`). The Detail page is where the owner sees all distribution surfaces (share link, embed snippet, email integration, QR) — the modal does not duplicate them.

Activation gates: (a) `Survey.questions.length >= 1`, (b) all required fields complete, (c) consent override (if present) attested.

### §7. Detail page — 3 collapsible sections

- **Path**: `/admin/surveys/[id]`.
- **Layout**: single vertical layout. Three sections stacked top-to-bottom, all collapsible. Same shell regardless of survey state. Header chrome (breadcrumb, status pill, audit-trail badge if consent overridden, Edit + More buttons) is in identical position across DRAFT / ACTIVE / PAUSED / STOPPED.
- **All collapsibles use the platform-standard chevron** (`▼` rotating -90deg when collapsed) — same icon set as Organization Settings.

The three sections:

1. **Distribution** — wide compact bar with the 4 distribution surfaces inline (Share link · Embed snippet · Email integration · QR code). Each tile shows a one-line value and a Copy/Manage/Generate action.
   - **Default expanded** when `responsesCount === 0` (no responses yet — the JTBD is share).
   - **Default collapsed** when responses exist (the JTBD is understand; the operator can still toggle open to grab a link).
   - The owner can override either default with the chevron at any time.
2. **Response** — placeholder block in #241 V0. Response analytics (score distribution, sentiment, topic clusters, individual response stream, LoopMonitor) are designed under a future sub-issue (sibling to #235), out of scope here. The placeholder describes what will land.
3. **Configuration summary** — collapsible, default collapsed. Shows the **actual rendered survey form** (the Look & Feel `preview-survey` component) as a first-class element on the left, with a compact text summary on the right (basics / look & feel / points / consent). The "as respondents see it" framing means the operator can verify post-activation that the form looks correct without re-opening the editor.

The Edit button in the detail header takes the owner back to the editor. The More menu opens Stop / Restart / Discard depending on state.

## Functional Requirements

The behaviors above expressed as testable SHALL statements with `Given / When / Then` acceptance criteria. Tags `R1`–`R28` are referenced from implementation tasks, tests, and the RFC's traceability matrix.

### Epic Acceptance Criteria — traceability map

The [#241 issue body](https://github.com/mathursrus/CustomerEQ/issues/241) lists 7 epic-level Acceptance Criteria. Each maps to a covering R# / decision below — or is flagged as superseded with a Decision Log pointer.

| Epic AC (verbatim from issue body) | Coverage |
|---|---|
| Wizard never produces duplicate drafts under any navigation pattern. | **Covered**: R4 (auto-save on blur; tab navigation does not POST). Validation Plan row: "Browser regression — The list page never produces duplicate drafts under back/forward navigation." |
| Survey detail page has Edit button for DRAFT/PAUSED; breadcrumb to `Surveys` everywhere. | **Covered**: §7 (detail page layout) + R26 (3 collapsible sections with header chrome). Edit button visible in §7 header for all states; breadcrumb in same header. |
| Stop/Restart show confirmation modals; "Close" verb is gone. | **Covered**: §5 vocabulary table + R25 (verb constraint). Modals are part of the §5 transitions (modal-confirmed annotations on Stop / Restart rows). |
| Vocabulary is uniform: "Activate" / "Stop" / "Save Draft" / "Restart" — "Launch" is removed. | **Covered**: §5 + R25 (UI strings SHALL never use "Launch" or "Close" for state operations). |
| Action-type dropdown contains only types that work end-to-end (others removed until built). | **Superseded by D14** — Rules / Post-Survey Actions deferred entirely from #241 V0 (no dropdown to enforce against). When the Rules tab ships in a future iteration (under sub-issues #234 / #242 / #246), the iteration owns this AC as its own R# requirement: "the Rules-tab action picker SHALL show only types whose worker executor + delivery integration have shipped to production." Cross-reference recorded here so the AC is not assumed missed. |
| Setting `Survey.incentivePoints` credits the configured currency on response submission via programs/campaigns; the `survey_completion` EarningRule path is gone. | **Superseded by D40** (R5 architectural reversal). The Epic issue body's original direction (Survey.incentivePoints canonical; EarningRule survey-completion path removed) was inverted: programs own earning; `EarningRule(triggerEvent='survey_completion')` is canonical; `Survey.incentivePoints` is dropped. The replacement AC, captured in R22 + §4: "On accepted response, the response handler SHALL emit a `survey_completion` event; the worker SHALL evaluate matching `EarningRule(triggerEvent='survey_completion')` for the survey's program and write a `LoyaltyEvent(action=EARN)` atomically with `Member.pointsBalance` increment." See Decision Log D40 for the full rationale. The issue body's AC text is stale and SHALL be updated on issue when the Epic closes. |
| Re-running the empirical reproduction from #225 yields a non-zero `LoyaltyEvent`. | **Covered**: R22's Given/When/Then maps the reproduction directly; also a Validation Plan row ("Compliance — Re-running the empirical reproduction from #225's LoyaltyEvent-zero bug yields a non-zero LoyaltyEvent after response submit"). Independent shippable fix tracked under [#315](https://github.com/mathursrus/CustomerEQ/issues/315) so the bug fix is not gated by #241's editor work. |

### Editor structure

| ID | Requirement | Acceptance Criterion (Given / When / Then) |
|----|-------------|---------------------------------------------|
| **R1** | The survey list page SHALL provide one primary `+ New survey` CTA. **The old entry-point routes SHALL be deleted (not redirected):** `/admin/surveys/new` (adhoc form), `/admin/surveys/new` (triggered wizard variant), `/admin/survey-builder` (with and without `surveyId` query param). Anyone navigating to a deleted route gets a 404 with a link back to `/admin/surveys`. Bookmarks held by existing operators retrain to the new flow once. | Given the admin lands on `/admin/surveys`, when they click `+ New survey`, then a new `Survey` row is created with `status = DRAFT` and the browser navigates to `/admin/surveys/[id]/edit` on the Basics tab. Given the admin navigates to `/admin/surveys/new` directly after V0 ships, when the route resolves, then a 404 page renders with a "Surveys" home link. |
| **R2** | A clickable row on the surveys list SHALL open the read-only detail page at `/admin/surveys/[id]`. Row-end actions SHALL be Edit (✎) and More (⋯). | Given the admin clicks a survey row's body, when navigation completes, then `/admin/surveys/[id]` renders read-only. Given the admin clicks `✎`, when navigation completes, then `/admin/surveys/[id]/edit` renders. |
| **R3** | The editor SHALL be a single section-tabbed page with exactly four tabs in this order: Basics → Questions → Look & Feel → Points & Thank You. | Given the admin opens the editor, when the page renders, then four tab headers are visible in the named order, and exactly one tab body is visible at a time. |
| **R4** | Field edits SHALL auto-save on blur (debounced) to the same `Survey` row created at draft time. Tab navigation SHALL NOT issue a POST that creates an additional row. | Given the admin types into a field and tabs away, when 500ms passes, then a PATCH `/v1/surveys/:id` is issued and the auto-save indicator reads "Saved · Xs ago". Given the admin clicks Next / Back, when navigation completes, then `SELECT count(*) FROM "Survey" WHERE id = :id` remains exactly 1. |
| **R5** | Every tab body SHALL provide `Back` and `Continue` buttons at the bottom; the primary `Activate` button SHALL be persistent in the page header. | Given any tab is active, when the page renders, then both bottom navigation buttons are visible (Back is disabled on the first tab; Continue label updates to "Activate →" on the last tab), and the header `Activate` button is visible at all times. |

### Basics tab

| ID | Requirement | Acceptance Criterion (Given / When / Then) |
|----|-------------|---------------------------------------------|
| **R6** | The Survey type field SHALL be a 4-card grid: NPS / CSAT / CES / Custom. Selecting NPS / CSAT / CES SHALL auto-populate the Questions tab with that type's standard preset set (all editable, all reorderable, all deletable). Custom SHALL leave the Questions canvas blank. **Type-change behavior**: when a survey already has questions and the admin changes Type, the editor SHALL prompt with a modal — "Change type to <X>? This will replace your current questions with the <X> preset. (Or cancel and keep your questions.)" Cancel keeps the existing type and questions; confirm replaces the canvas with the new preset and discards the prior questions. **Custom type** SHALL never trigger the modal (Custom is "keep whatever questions exist or start blank"). | Given the admin starts with NPS preset (2 questions populated) and changes Type to CSAT, when the type-change confirmation modal appears, then the admin sees the change-or-cancel choice. Given the admin confirms, then the Questions canvas now shows the CSAT preset. Given the admin cancels, then Type remains NPS and the 2 NPS questions remain. Given the admin picks Custom while having NPS questions, then no modal fires and the questions remain. |
| **R7** | Internal name (`Survey.name`) SHALL be required and admin-facing only. Survey title (`Survey.title` — new nullable column) SHALL be required and respondent-facing. | Given either field is empty, when the admin attempts Activate, then validation blocks activation and the failing field is highlighted in Basics. Backfill: existing rows have `title` populated to `name` value. |
| **R8** | The Response policy field SHALL be a single-select dropdown with three options: Allow multiple responses (default) / One response per member / Latest overwrites prior. **Anonymous responses** (`SurveyResponse.memberId IS NULL`) SHALL bypass response-policy enforcement entirely — every anonymous submission is stored as a new row regardless of the configured policy, because there is no member identity to enforce uniqueness against. **Second-submit under ONCE**: when an identified member submits a second response to a survey with `responsePolicy='ONCE'` and a prior accepted response exists for that member, the API SHALL return HTTP 409 and the respondent SHALL see "You've already answered this survey. Thank you for your prior response." No new SurveyResponse row is written; no LoyaltyEvent fires. | Given a brand has surveys created before this Epic, when the migration runs, then every existing `Survey.responsePolicy` is `MULTIPLE` (matches #231 R14). Given `responsePolicy='ONCE'` and an anonymous respondent submits twice, when both submissions complete, then two distinct SurveyResponse rows exist (both with `memberId=NULL`). Given `responsePolicy='ONCE'` and member `mbr_abc` already has an accepted response, when member `mbr_abc` submits again, then the API returns HTTP 409 with the "already answered" message and `SELECT count(*) FROM SurveyResponse WHERE memberId='mbr_abc' AND surveyId=:s` remains 1. |

### Consent collection (sub-block in Basics)

| ID | Requirement | Acceptance Criterion (Given / When / Then) |
|----|-------------|---------------------------------------------|
| **R9** | The Consent mode dropdown SHALL default to "Inherit brand default · `<resolved mode>`". An override option SHALL appear only when it *differs* from the brand default (per #283 binding decisions). | Given `Brand.consentMode = EXPLICIT`, when the editor renders the consent sub-block, then the dropdown shows "Inherit brand default · Explicit consent required" plus one override option "Implied on submit"; no other override appears. |
| **R10** | Setting the consent mode to a *more permissive* value than the brand default SHALL require attestation (auto-filled identity, free-text reason ≤500 chars required, attestation checkbox) before save. The API SHALL return HTTP 422 if attestation or reason is missing. | Given brand is Explicit and the survey owner selects "Override · Implied on submit" and clicks Save without filling the modal, when the API receives the PATCH, then the response is 422 and no audit row is written. Given the admin completes the modal and clicks Save, then 200 OK, audit row written with `metadata.consentMode = 'IMPLIED_ON_SUBMIT'` and `metadata.consentReason` populated. |
| **R11** | Setting consent mode to a *stricter* value than the brand default SHALL NOT require attestation but SHALL still write an audit row. | Given brand is Implied and the survey owner picks "Override · Explicit consent required" and clicks Save, when the API processes it, then 200 OK with audit row written and no attestation modal fired. |
| **R12** | The disclosure-text override SHALL provide a `.consent-toolbar` with `[+] Privacy link` and `[+] Terms link` buttons that insert tokens (`{{privacy:"…"}}` / `{{terms:"…"}}`) at the cursor — identical to the Organization Settings editor. The Terms link button SHALL be rendered only when `Brand.termsUrl IS NOT NULL`. | Given `Brand.termsUrl IS NULL`, when the consent sub-block renders, then the Terms link button is not visible (the Privacy link button still is). Given the admin clicks Privacy link with no selection, then `{{privacy:"Privacy Policy"}}` is inserted at the cursor and the preview-card updates. |
| **R13** | A **blank** disclosure text (empty `Survey.consentTextOverride` AND empty `Brand.consentTextDefault` fallback) SHALL render no consent block on the survey form. The audit log SHALL capture the blank-disclosure setting on save with actor + timestamp + new value. Survey creator bears the legal authorization onus. | Given the admin clears the disclosure textarea and saves, when the response form renders, then no consent UI is shown and an audit row is written with `metadata.consentTextOverride = ''`. Use case: respondents outside GDPR/CCPA scope (e.g., Washington state). |
| **R14** | The Preview card SHALL render the disclosure exactly as the respondent will see it: with checkbox under Explicit mode; paragraph form (no checkbox) under Implied mode; empty (no consent block at all) when the disclosure text is blank. | Given Explicit mode + non-empty disclosure, when the preview renders, then a checkbox + disclosure paragraph appears. Given Implied + non-empty disclosure, paragraph only. Given blank disclosure, then the preview body is empty. |

### Audience: member identification (channel-aware)

| ID | Requirement | Acceptance Criterion (Given / When / Then) |
|----|-------------|---------------------------------------------|
| **R15** | Standalone-distributed surveys SHALL render a member-identification input field on the form, based on the brand's `memberIdentifierKind` setting from Organization Settings (#277). The member SHALL provide it before submit. | Given `Brand.memberIdentifierKind = 'email'` and the survey is rendered via share link, when the response form loads, then an email input field appears above the questions and is required for submit. The Standalone preview in Look & Feel reflects this exactly. |
| **R16** | Embedded-distributed surveys (widget JS mounted on host page) SHALL consume member identity from the host page out-of-URL (per D51 / OQ4 resolution — absorbs #209's privacy fix). Two integration patterns supported: **(A1) data-attribute prefill** — `<script src="…/widget.js" data-survey="srv_abc" data-prefill-email="<value>" data-prefill-external-id="<value>" data-prefill-phone="<value>" data-prefill-first-name="<value>" data-prefill-last-name="<value>"></script>` (one-paste; values templated server-side by the brand). **(A2) JS prefill API** — `CustomerEQ.surveys.prefill('srv_abc', { email, externalId, phone, firstName, lastName })` after widget mount (for SPA contexts). If neither pattern provides identity, the form falls back to rendering the same identification prompt as Standalone (R15). **No URL identifier parameter SHALL be supported in V0** — the existing `?email=` / `?member_id=` URL surface from pre-#241 code is removed. Identifier reaches the server via POST body, which `resolveOrEnrollMember` already consumes — so auto-enroll (#231) works unchanged. | Given a host page mounts the widget with `data-prefill-email="jane@example.com"`, when the form renders, then no identification field is shown and the POST body on submit carries `email='jane@example.com'`. Given the host page calls `CustomerEQ.surveys.prefill('srv_abc', { externalId: 'usr_xyz' })` after mount, when the user submits, then POST body carries `externalId='usr_xyz'`. Given neither prefill pattern is used, when the form renders, then the identification field is shown per R15. Given a legacy embed snippet that still passes `?email=` in the URL after V0 ship, when the widget loads, then the URL param is ignored (deprecated path; documented as removed) and the form falls back to R15 prompt. |

### Look & Feel

| ID | Requirement | Acceptance Criterion (Given / When / Then) |
|----|-------------|---------------------------------------------|
| **R17** | The Look & Feel preview SHALL be channel-first: top tabs `Standalone (link)` and `Embedded (widget)`. Within each tab, Desktop and Mobile previews SHALL render side-by-side. | Given the admin clicks the Embedded top tab, when the preview renders, then both a Desktop and a Mobile rendering of the embedded mode are visible side-by-side; the Standalone grid is hidden. |
| **R18** | The Per-channel chrome matrix SHALL provide three rows (Brand logo / Brand name / Survey title) and two columns (Standalone / Embedded) of toggles. The form-renderer SHALL respect the matrix at render time. Defaults: full chrome on Standalone, only Survey title on Embedded. | Given a survey has `chromeMatrix = { standalone: { logo: true, name: true, title: true }, embedded: { logo: false, name: false, title: true } }`, when the form renders via embed widget, then no logo and no brand name appear; the survey title appears. |
| **R19** | The Theme picker SHALL list every theme defined for the brand (no count cap) and SHALL NOT include a "Manage themes" affordance — theme creation / editing lives in Organization Settings (per RBAC: the survey creator may not have theme-edit access). | Given the brand has 12 themes, when the Look & Feel theme picker renders, then 12 cards are visible (wrapping rows as needed) and no `Manage themes →` link is present. |

### Points & Thank You

| ID | Requirement | Acceptance Criterion (Given / When / Then) |
|----|-------------|---------------------------------------------|
| **R20** | Points awarded for completion SHALL be a read-only display in the editor, sourced from `EarningRule(programId = Survey.programId, triggerEvent = cxEventFor(Survey.type))` where `cxEventFor(type)` maps `NPS → 'cx.nps_response'`, `CSAT → 'cx.csat_response'`, `CES → 'cx.ces_response'`, `CUSTOM → 'cx.survey_completed'` (per D50). When the program has no matching EarningRule for the survey's specific cx event, the row SHALL read "No points configured for <type> survey completion" with a pointer for the program admin. **Program rate changes after survey activation**: the editor display SHALL reflect the program's current rate at page load; existing `LoyaltyEvent` rows already credited SHALL retain their original `pointsEarned` value (no retroactive recalculation). | Given an NPS survey under a program with `EarningRule(triggerEvent='cx.nps_response', pointsAwarded=50, status=ACTIVE)`, when the Points & Thank You tab renders, then the row reads "50 \<currency\> per accepted NPS response — set in \<program\> program" with no editable input. Given a CSAT survey under the same program with no CSAT-specific rule, then the row reads "No points configured for CSAT survey completion" — even if an NPS rule exists, because triggerEvent must match exactly. Given the program admin changes the NPS rule's `pointsAwarded` from 50 to 100 after 200 responses already credited at 50, when a new NPS response is submitted, then that response credits 100; the prior 200 `LoyaltyEvent` rows are unchanged. |
| **R21** | The Thank-you message variable picker SHALL offer V0 variables only: `{{points}}`, `{{pointCurrencyName}}`, `{{rewardLink}}`. The variables `{{programName}}` and `{{memberName}}` SHALL NOT appear. | Given the admin clicks the variable-picker, when the chips render, then exactly three chips are visible: points, pointCurrencyName, rewardLink. The default thank-you copy contains only V0 variables. |
| **R22** | On accepted response, the response handler SHALL emit exactly **one** event whose `eventType` is the cx event for the survey's type — `cx.nps_response` for NPS, `cx.csat_response` for CSAT, `cx.ces_response` for CES, `cx.survey_completed` for Custom (per D50 OQ5 resolution). The worker SHALL evaluate matching `EarningRule(programId = Survey.programId, triggerEvent = <emitted-cx-event>)` and write a `LoyaltyEvent (action=EARN)` atomically with `Member.pointsBalance` increment per project rule R7. The same emission feeds the existing `SurveyRule` → Campaign trigger pipeline (#80). | Given an NPS survey under a program with `EarningRule(triggerEvent='cx.nps_response', pointsAwarded=50)`, when the member submits a response and the worker completes processing, then `SELECT count(*) FROM LoyaltyEvent WHERE memberId = :m AND eventType = 'cx.nps_response' AND payload->>'surveyId' = :s` is 1 AND `Member.pointsBalance` has advanced by 50. Given the program admin configures `cx.nps_response: 50, cx.csat_response: 20` and a member completes one of each, then two LoyaltyEvent rows are written (50 + 20). Idempotency: a second submit with the same `idempotencyKey` does not double-award. **#315's scope folds into this requirement** — there is no separate `survey_completion` event to wire; the bug fix is "ensure the cx event already emitted is consumed by the EarningRule evaluator", which is true today as long as the rule's `triggerEvent` matches the emitted event verbatim. |

### State transitions + activation

| ID | Requirement | Acceptance Criterion (Given / When / Then) |
|----|-------------|---------------------------------------------|
| **R23** | `Activate` SHALL be gated on (a) `Survey.questions.length >= 1`, (b) all required fields complete, (c) consent override (if any) attested. Activation SHALL redirect to `/admin/surveys/[id]` on success. | Given a survey has zero questions, when the admin clicks Activate, then the gating fires and the editor jumps to Questions with an inline message. Given all gates pass and the admin confirms the activate modal, then `Survey.status` advances DRAFT→ACTIVE and the browser navigates to the detail page. |
| **R24** | Every survey state transition (Activate / Pause / Resume / Stop / Restart / Discard / Delete) SHALL write an audit-log entry via the existing audit plugin with `{ actorUserId, surveyId, fromStatus, toStatus, timestamp }`. | Given the admin stops a survey, when the PATCH `/v1/surveys/:id/status` returns 200, then an audit-log row exists with `action = 'survey.update'`, `metadata.fromStatus = 'ACTIVE'`, `metadata.toStatus = 'STOPPED'`, `actorUserId` set. |
| **R25** | Schema rename: `SurveyStatus.CLOSED → STOPPED`. UI strings SHALL never use "Launch" or "Close" for state operations; the only verbs SHALL be Save Draft, Activate, Stop, Restart, Pause, Resume, Discard. | Given the migration runs, when complete, then `SELECT count(*) FROM "Survey" WHERE status = 'CLOSED'` is 0 and no UI string in `apps/web` contains the regex `\bLaunch\b|\bClose\b` in a state-action context (grep verified pre-merge). |
| **R29** | **Editability after Activate**: a survey in ACTIVE / PAUSED / STOPPED state SHALL remain editable from the editor for the fields whose change does not violate response-data integrity. Editable in ACTIVE: title, description, theme, look-and-feel chrome matrix, thank-you message, thank-you redirect URL, consent disclosure text (with audit log), consent mode override (with audit log + attestation per R10), trigger (V1+ when triggering ships). Non-editable in ACTIVE: name (internal label still editable; aliasing same field), type, program, response policy, questions list / question text. Non-editable fields SHALL be visually marked (read-only inputs with a small "Editable only when in DRAFT" tooltip) and reject API writes with HTTP 409. PAUSED behaves identically to ACTIVE. STOPPED is fully non-editable (no further responses incoming; admin uses Restart to resume editing if needed). | Given a survey is ACTIVE and the admin edits the thank-you message, when they tab away, then the auto-save PATCH succeeds (200) and the new copy is shown on next response submission. Given a survey is ACTIVE and the admin attempts to change the survey type via the API, when the PATCH lands, then HTTP 409 is returned with body `{ "code": "FIELD_NOT_EDITABLE_IN_STATE", "field": "type", "currentState": "ACTIVE" }`. |
| **R30** | **Basics-tab editability when responses exist**: independent of state (R29), once `Survey.responsesCount > 0`, the response-policy field SHALL become read-only with a tooltip "Cannot change response policy after responses are collected" — switching the policy retroactively would invalidate prior responses' interpretation. All other Basics fields follow R29's state-based editability. | Given a survey is DRAFT with 0 responses, when the admin edits response policy, then the dropdown is editable and the PATCH succeeds. Given the same survey advances to ACTIVE with 5 responses, when the admin opens the editor, then response policy renders read-only; PATCH attempt returns HTTP 409. |
| **R31** | **Theme tokens map to specific survey-form elements**: the SurveyTheme record (post-#291 split) defines `primaryColor`, `backgroundColor`, `textColor`, `headingFont`, `bodyFont`, `borderRadius`. The form-renderer SHALL apply them as: primaryColor → submit button background + scale-cell selected state + brand-logo accent; backgroundColor → form background; textColor → all body copy + question text; headingFont → survey title; bodyFont → questions, consent disclosure, thank-you message; borderRadius → form container + input borders + submit button. Brand logo asset (`BrandTheme.logoUrl` from #291) is used by R18's chrome matrix. | Given a SurveyTheme has `primaryColor='#16a34a'`, when the form renders, then the submit button background is `#16a34a` and the selected-scale-cell uses the same value. Given the SurveyTheme is changed to `primaryColor='#4f46e5'`, when the form re-renders, then the same two elements update; no other elements re-color. Validation Plan adds a visual-regression test asserting per-element token application. |

### Detail page

| ID | Requirement | Acceptance Criterion (Given / When / Then) |
|----|-------------|---------------------------------------------|
| **R26** | The detail page SHALL have exactly three vertical sections in this order: Distribution, Response, Configuration summary. All three SHALL be collapsible using the platform-standard `▼` chevron (rotates -90deg when collapsed). | Given the admin lands on `/admin/surveys/[id]`, when the page renders, then three section headers are visible in the named order; each has a chevron toggle that collapses/expands its body. |
| **R27** | The Distribution section SHALL default expanded when `responsesCount === 0` and collapsed when `responsesCount > 0`. The admin SHALL be able to override either default via the chevron at any time. | Given `responsesCount = 0`, when the page first renders, then Distribution is expanded. Given `responsesCount > 0`, when the page first renders, then Distribution is collapsed (and a single click on the chevron expands it). |
| **R28** | The Configuration summary section SHALL render the actual configured survey form as a first-class element (reusing the Look & Feel `preview-survey` component) on the left, with a compact text summary on the right. **Default expand/collapse** follows the same pattern as Distribution: expanded when `responsesCount === 0` (admin wants to verify the configuration before sharing); collapsed when `responsesCount > 0` (admin's focus shifts to Response analytics). The admin SHALL be able to override either default via the chevron at any time. | Given the admin expands Configuration summary, when the section body renders, then a rendered preview of the survey form appears alongside dl-style text summary entries for basics / look & feel / points / consent. Given `responsesCount = 0`, when the detail page first renders, then Configuration summary is expanded. Given `responsesCount > 0`, when the detail page first renders, then Configuration summary is collapsed. |
| **R32** | **The Response section SHALL default collapsed when `responsesCount === 0` and expanded when `responsesCount > 0`** — inverse of Distribution (R27) and Configuration summary (R28). When zero responses, there's nothing to show beyond the placeholder; when responses exist, the analytics surface (designed under a future sibling sub-issue to #235) becomes the primary content. The admin SHALL be able to override either default via the chevron. | Given `responsesCount = 0`, when the detail page renders, then Response section is collapsed (body hidden). Given `responsesCount > 0`, when the detail page renders, then Response section is expanded (analytics block visible — placeholder block in #241 V0, real analytics in the sibling sub-issue). |

### Open Questions (RFC / implementation phase)

These items were unresolved at draft-time; reviewer answers captured below. Open Questions added in later rounds remain open until the reviewer responds.

- **OQ1 — RESOLVED (R9 review)**: silent skip in V0. Reviewer (PR #314 comment id `3216675798`): "This should always be a silent skip. Not every survey may have reward points associated." The editor's "No points configured" indicator pre-warns the admin; no runtime warning surface needed.
- **OQ2 — RESOLVED (R9 review)**: restrict to the survey's program. Reviewer (PR #314 comment id `3216679703`): "Need to restrict to the Survey's program." Worker SHALL evaluate only `EarningRule(programId = Survey.programId, triggerEvent='survey_completion')`; multi-program enrollment cases write at most one LoyaltyEvent per response.
- **OQ3 — RESOLVED (R9 review)**: defer Campaign-multiplier layering to a future enhancement. Reviewer (PR #314 comment id `3216682000`): "Agreed with the recommendation." V0 ships the base EarningRule path; Campaign multipliers will be designed under a separate sub-issue when demand surfaces.
- **OQ4 — RESOLVED (R11 review)**: **JS / data-attribute prefill, no URL identifier (absorbs #209).** Reviewer flagged that the originally-recommended opaque-token approach breaks #231's auto-enroll path — there's no token to issue for first-time visitors who haven't been enrolled yet. Five alternatives evaluated (JS prefill / brand-issued distribution refs / signed JWT in URL / hash-based identifier / first-party cookie); reviewer picked **JS prefill**. Resolution detail: R16 amended to support two prefill patterns — **(A1)** data-attribute on the embed script tag (server-rendered brands keep one-paste ergonomics); **(A2)** `CustomerEQ.surveys.prefill(...)` API call (SPA brands). No URL identifier parameter in V0. #231 auto-enroll is unaffected because identifier reaches the server via POST body, which `resolveOrEnrollMember` already consumes — verified at `apps/api/src/routes/public.ts:295` (call signature accepts `memberId / email / phone / firstName / lastName` from POST body fields). #209's privacy concern is fully addressed: no URL surface for PII. **Deferred to future enhancements** (listed in Out of Scope): (i) **brand-issued distribution refs** (Option B) — `POST /v1/distributions` API + token table for per-invitation tracking analytics; (ii) **encrypted URL-token integration mode** — symmetric / asymmetric key relationship per brand, encrypted blob in URL, decrypted server-side, for brands that can't use JS prefill in their stack. See D51.
- **OQ5 — RESOLVED (R10 review)**: **single event, type-aware EarningRule trigger.** Reviewer pushback (PR #314 comment after `3216773456`): "Survey creation has one entry point — this issue. Where else do we expect the other cx events to be raised from? Survey knows the type of survey it is: nps survey, csat survey, ces survey or custom survey. Shouldn't it emit the corresponding event based on survey type completed. This can then be tied to program creation giving admin the flexibility to award different points for each type of survey. I don't see the point of creating a new survey_completion event." **Resolution**: drop the `survey_completion` event entirely. The response handler emits exactly one event per accepted response (the existing CX events: `cx.nps_response` / `cx.csat_response` / `cx.ces_response` / `cx.survey_completed`). EarningRule trigger configuration uses these cx event strings directly — `EarningRule(programId, triggerEvent='cx.nps_response', pointsAwarded=N)` is the canonical shape. The worker's existing exact-string-match evaluator (verified at `apps/worker/src/processors/loyaltyEvents.ts:43-105`) needs no change. **Side benefit**: program admins now naturally get per-survey-type points configuration — one program can award 50 Stars for NPS, 20 for CSAT, 30 for CES, 25 for Custom by creating four EarningRule rows. This affects R22, §4 Single earning path, NFR-R2, the migration plan, and the editor's Points & Thank You display — all amended in this commit. See D50.

### Error States

Per the `requirement-extraction` skill ("explicit edge cases for invalid input, empty states, and failure handling"). Each row maps to the requirement that owns it or names a new edge.

| Scenario | Behavior | Owning requirement |
|---|---|---|
| Activate clicked with `Survey.questions.length === 0` | Activation gated; editor jumps to Questions tab with inline message "Add at least one question before activating." | R23 |
| Activate clicked with required Basics fields empty (name / title / program / type / response policy) | Activation gated; editor jumps to Basics tab with field-level error markers; activate modal does not open. | R23 |
| Consent override PATCH without attestation flag or reason text | API returns HTTP 422; attestation modal stays open with field-level error; no audit row written. | R10 |
| Cross-brand PATCH access attempt | API returns HTTP 403; no row mutation; audit row written for the unauthorized access attempt. | NFR-S1 / project rule R6 |
| Disclosure text override > 500 chars | Client-side: textarea max-length 500 with live character counter; server validates same on PATCH. | new edge — covered by R12 amendment |
| Embed widget mount with neither A1 data-attribute prefill nor A2 JS `prefill()` call | Form falls back to rendering the same member-ID prompt Standalone surveys use (graceful degradation per R16). Auto-enroll runs normally against the form-submitted identifier. | R16 |
| Legacy embed snippet still using `?email=` / `?member_id=` URL param after V0 ship | URL param is ignored (deprecated path removed per D51); form falls back to R15 prompt; one-time documentation nudge for brand integrators to migrate to A1 or A2. | R16 |
| Response submit when `responsePolicy === 'ONCE'` and member already submitted | API returns HTTP 409 with respondent-facing message "You've already answered this survey." No new SurveyResponse row written. | Inherits from #231 |
| Response submit when `responsePolicy === 'LATEST_OVERWRITES'` and prior response exists | API updates the prior row; emits the cx event for the survey's type with `idempotencyKey` advancing so the matching EarningRule fires once per submission. | R22 / NFR-R2 |
| Program has no `EarningRule(triggerEvent=cxEventForType(Survey.type))` AND member submits a response | Worker silently skips earning (no LoyaltyEvent row, no error) per OQ1 resolution. The editor's Points & Thank You row pre-warns the admin via "No points configured for *<type>* survey completion." | OQ1 / R20 |
| Auto-save PATCH fails (network error) | Editor shows a non-blocking "Save failed — retrying…" indicator; retries with exponential backoff up to 5 attempts; preserves user edits in browser state across retries. After 5 failures, surfaces a blocking error with "Copy edits to clipboard" affordance. | NFR-R3 |
| Theme picker has zero themes (brand newly created, no defaults provisioned) | Falls back to a system-default theme; banner: "Your brand has no themes yet — set them up in Organization Settings." Survey remains saveable; no editor-blocking error. | new edge — covered by R19 amendment |
| Stop / Restart / Pause issued from a state that doesn't permit it (e.g., Restart on DRAFT) | More-menu only renders the transitions valid for the current state (per R26 menu shape); API also returns HTTP 400 if a route is hit directly. | R26 + R24 |

## Non-Functional Requirements

The behaviors above are functional; this section captures the constraints they must operate under. Tags `NFR-*` are grouped by category and referenced from the RFC's architecture-decisions matrix.

### Performance

| ID | Requirement | Target |
|----|-------------|--------|
| **NFR-P1** | Editor auto-save PATCH response time | p95 < 200ms; p99 < 500ms (single-field save). Measured at the API boundary. |
| **NFR-P2** | Survey form initial render (standalone, cold load) | p95 < 2s on a representative 3G mobile profile; p99 < 4s. Measured via the standalone `/s/<surveyId>` route. |
| **NFR-P3** | Embed widget mount + first paint | p95 < 1s post-DOMContentLoaded on host page. Self-contained bundle; no external runtime dependencies. |
| **NFR-P4** | Response submit → `LoyaltyEvent` write | p95 < 30s end-to-end (well within Issue #6's 15-min feedback-to-action SLA per CLAUDE.md). |
| **NFR-P5** | Activate gating check (client-side validation of question count + required fields) | < 100ms locally; never hits API. |

### Security

| ID | Requirement | Control |
|----|-------------|---------|
| **NFR-S1** | Every Survey read / write SHALL be brand-scoped | Per project rule R6 — `brandId` sourced from verified JWT, never accepted from request body. Already enforced in `apps/api/src/routes/surveys.ts` (36 `brandId` references verified). The consent-override PATCH endpoint absorbed from #283 uses the same gate. |
| **NFR-S2** | Survey IDs SHALL be opaque (unguessable) | `Survey.id` is a Prisma cuid (already in schema). Share links use the full cuid; no sequential surface. |
| **NFR-S3** | Disclosure text override SHALL NOT permit raw HTML | Server validates: only `{{privacy:"<label>"}}` and `{{terms:"<label>"}}` tokens are permitted to produce HTML output (resolved to `<a>` tags); all other text is escaped at render time. Prevents XSS via disclosure text. |
| **NFR-S4** | Embed widget SHALL run with no host-page privilege escalation | Widget JS served from CustomerEQ origin; iframe runs in host page context but does not require host cookies / localStorage. Member identity reaches the widget via the host page's own server-rendered data-attributes (A1) or in-process JS call (A2) per R16 / D51 — never via URL, never via cross-origin storage. |
| **NFR-S5** | Audit-log writes for consent-mode override and disclosure-text changes SHALL include `actorUserId`, `requestIp`, and request timestamp | Existing audit plugin captures these for the routes it covers. `requestIp` is sourced from Fastify's `request.ip` (which respects the configured trust-proxy chain). If the proxy chain is misconfigured and IP is unavailable, the audit row is still written with `requestIp = null` and a structured-log warning is emitted so the misconfiguration is observable. The audit row is never blocked on IP availability. |

### Reliability

| ID | Requirement | Control |
|----|-------------|---------|
| **NFR-R1** | `LoyaltyEvent` + `Member.pointsBalance` SHALL update atomically | Per project rule R7. Single Prisma `$transaction` wraps both writes in the existing worker handler `apps/worker/src/processors/loyaltyEvents.ts:250-267` (verified). |
| **NFR-R2** | Response submit SHALL be idempotent | One event per response (per D50 OQ5 resolution); idempotency key `survey:${surveyId}:${memberId}` (existing). A second submit with the same key is rejected by the queue; no double-credit on retry. The previous design (two events, two keys) is obsolete. |
| **NFR-R3** | Auto-save SHALL be resilient to transient network failures | Network failure → non-blocking "Save failed — retrying…" indicator; exponential backoff up to 5 attempts; user edits preserved in browser state across retries (see Error States table). |
| **NFR-R4** | State-transition consistency | All `Survey.status` writes go through `PATCH /v1/surveys/:id/status` (existing single endpoint, verified at `surveys.ts:126`). No client-side status writes. |
| **NFR-R5** | Survey-response event pipeline SHALL handle backpressure and worker downtime gracefully | The cx event (per D50: one event per response, `cx.<surveytype>_response`) emits to the existing BullMQ queue (per `apps/api/src/routes/public.ts` enqueue pattern). When the worker is down or the queue is backed up, the event is persisted and processed when capacity returns — the response is still accepted at the API and the `SurveyResponse` row is written synchronously; only the EarningRule evaluation + LoyaltyEvent write is asynchronous. Failed jobs go to the existing BullMQ dead-letter mechanism (standard configuration; no new DLQ surface introduced). Recovery: when the worker comes back, queued events drain in FIFO order; idempotency key per NFR-R2 prevents double-credit on retry. |

### Scalability

| ID | Requirement | Target |
|----|-------------|--------|
| **NFR-SC1** | Editor SHALL remain interactive with up to 200 questions per survey | Drag-drop canvas, question selection, and right-rail config updates all sub-100ms locally with `Survey.questions.length = 200`. |
| **NFR-SC2** | Theme picker SHALL scale with brand-theme count | Renders up to 100 themes in the card grid without lazy-loading or pagination. Beyond 100, a "Search themes" field becomes visible (deferred until a brand approaches the limit). |
| **NFR-SC3** | Surveys list SHALL paginate | Existing pageSize=25 cursor pagination (unchanged). |
| **NFR-SC4** | Response submission throughput SHALL accommodate burst loads | The existing BullMQ queue + worker pipeline handles back-pressure; cx events (per D50: one event per response, `cx.<surveytype>_response`) emit to the same queue. No new scalability surface introduced by #241. |

### Accessibility (WCAG 2.1 AA target)

| ID | Requirement | Control |
|----|-------------|---------|
| **NFR-A1** | Editor SHALL be fully keyboard-navigable | Tab order: page-header → horizontal tabs → tab body → Back/Continue → top-right Activate. Roving-tabindex on the tab strip; Arrow keys cycle tabs. Drag-drop canvas operations have keyboard alternatives (Up/Down to reorder, Enter to select for edit). |
| **NFR-A2** | All controls SHALL have programmatic labels | Chrome-matrix toggles: `aria-label="<element> on <channel>"`. Consent-mode dropdown: `aria-describedby` pointing at the preview-card. Required fields: `aria-required="true"`. |
| **NFR-A3** | Color SHALL NOT be the only state indicator | Status badges include text labels ("Draft" / "Active" / "Stopped") alongside color. Audit-trail badge same. |
| **NFR-A4** | Respondent-side form SHALL meet WCAG AA contrast and label requirements | Member-identification field has explicit `<label>`; consent disclosure links keyboard-accessible; submit button has visible focus ring. |
| **NFR-A5** | Validation errors SHALL be programmatically associated with their fields | `aria-describedby` from input to error message; live region announces validation state changes for screen readers. |

### Observability

| ID | Requirement | Mechanism |
|----|-------------|-----------|
| **NFR-O1** | Audit log SHALL capture every Survey state transition | Per R24. `apps/api/src/plugins/audit.ts` extended (existing plugin, verified) — `metadata.fromStatus` + `metadata.toStatus` written on each PATCH status. |
| **NFR-O2** | Audit log SHALL capture every consent-mode override write | Per R10/R11. Includes `metadata.consentMode`, `metadata.consentReason`, `actorUserId`. |
| **NFR-O3** | Audit log SHALL capture every disclosure-text change | Per R13. Includes `metadata.consentTextOverride` (new value, truncated to first 200 chars) + actor + timestamp. |
| **NFR-O4** | Survey-completion event SHALL emit standard event metrics | Worker emits BullMQ consumer-lag, success-count, failure-count per existing observability pattern (no new metrics surface). |
| **NFR-O5** | Editor activity SHALL feed a future "survey activity" view | The audit log is the data substrate (per NFR-O1/O2/O3); the UI for the activity view is out of scope for #241. |

### Internationalization

| ID | Requirement | V0 posture |
|----|-------------|------------|
| **NFR-I1** | UI strings extractable for translation | **Deferred.** Verified `apps/web/package.json` includes no i18n library (`next-intl` / `react-intl` / `i18next` all absent). V0 keeps editor strings inline in English — NOT a regression vs. existing admin surfaces. Revisit when the platform adopts i18n broadly. |
| **NFR-I2** | Currency labels SHALL respect the program's configured currency name | Required at V0. `{{pointCurrencyName}}` variable in the thank-you message surfaces `Program.pointCurrencyName`. The Points & Thank You read-only display also uses this. |
| **NFR-I3** | Respondent-side form SHALL support RTL layout | Deferred to platform i18n adoption (NFR-I1). |

### Browser compatibility

| ID | Requirement | Target |
|----|-------------|--------|
| **NFR-B1** | Editor SHALL support latest 2 versions of Chrome / Safari / Firefox / Edge | Matches existing CustomerEQ admin browser support. |
| **NFR-B2** | Respondent-side form SHALL support latest 2 versions of Chrome / Safari / Firefox / Edge, plus iOS Safari (latest 2) and Android Chrome (latest 2) | Same form-renderer code path; verified via E2E suite browser matrix. |
| **NFR-B3** | Embed widget SHALL mount cleanly inside iframes, shadow-DOM hosts, and direct host DOM | Self-contained bundle with no external runtime dependencies; CSS uses BEM-style namespacing to avoid host-page collisions. |

### Backward compatibility & migrations

| ID | Requirement | Control |
|----|-------------|---------|
| **NFR-BC1** | Migrations SHALL be idempotent | Per #270 lessons — every migration safe to re-run. Enforced by `.github/workflows/ci.yml:75` (`pnpm db:migrate` step gating PRs; verified at file:line). Comment at `.github/workflows/ci.yml:70-72` cites #270 as the regression-prevention rationale. |
| **NFR-BC2** | Existing SurveyResponse rows SHALL be untouched by #241 migrations | Schema deltas (Survey.title add, Survey.incentivePoints drop, status enum rename) operate on the Survey table only. SurveyResponse rows are not modified. |
| **NFR-BC3** | All `/v1/surveys/*` API endpoint paths and HTTP verbs SHALL remain unchanged | Field shapes evolve (PATCH body now accepts `title`; `incentivePoints` removed from response shape). Existing clients without the new field continue to work; missing `title` is backfilled to `name` value at migration time so existing surveys render correctly. |
| **NFR-BC4** | The new `PATCH /v1/surveys/:id/consent-mode` endpoint (absorbed from #283) SHALL be additive | No existing endpoint is removed or renamed. |

## Schema and API Summary (RFC handoff aid)

Consolidated table of every schema delta and API surface change in #241, for the RFC author's reference.

### Schema deltas (Prisma migrations)

| Migration | Type | Description | Backfill |
|---|---|---|---|
| Add `Survey.title` | Add nullable column | `title String?` — respondent-facing form heading (D16) | Existing rows backfilled to `Survey.name` value |
| Drop `Survey.incentivePoints` | Drop column | Per D40 — earning lives on the program's EarningRule | Auto-create per-type `EarningRule(triggerEvent=cxEventForType(...), pointsAwarded=N, attribution='__migration_241__')` for brands with prior intent on Survey (per D50 OQ5 resolution — one rule per cx event type the program's surveys use) |
| Rename `SurveyStatus.CLOSED → STOPPED` | Enum rename | Per D5 / R25 — vocabulary consolidation | Existing rows with `status='CLOSED'` updated to `status='STOPPED'`; UI strings updated lockstep |
| Convert dead `EarningRule(triggerEvent='survey_completion')` rows (per D50) | **Fan-out + delete** | These rules never fired (the event was never emitted). For each such rule, create one EarningRule per cx event type the program's surveys actually use (preserves operator intent) with the same `pointsAwarded` and `__migration_241__` attribution; then delete the original row. | n/a (no original Survey field to backfill — the rows were dead) |
| Brands with no prior earning intent (no `Survey.incentivePoints > 0` AND no `EarningRule(triggerEvent='survey_completion')` AND no `EarningRule(triggerEvent='cx.*_response')`) | No-op | The migration drops `Survey.incentivePoints` from any rows it exists on; creates nothing new. Surveys remain valid; admin sees "No points configured for *<type>* survey completion" in the editor per R20. | n/a |

### API surface changes

| Endpoint | Verb | Change | Reference |
|---|---|---|---|
| `/v1/surveys` | POST | Unchanged path; request schema accepts `title` field | R7 |
| `/v1/surveys` | GET | Unchanged | R1 / R3 |
| `/v1/surveys/:id` | GET | Unchanged | R2 |
| `/v1/surveys/:id` | PATCH | Accepts `title` field; no longer accepts `incentivePoints` | R4 / R7 |
| `/v1/surveys/:id/status` | PATCH | Existing endpoint — now writes audit-log entries via plugin on every transition (per R24 / NFR-O1) | R23 / R24 |
| `/v1/surveys/:id/consent-mode` | PATCH | **NEW** (absorbed from #283) — writes `Survey.consentMode`, `consentSuppressedAttestedBy`, `consentSuppressedAttestedAt`, `consentReason`. Enforces R10 attestation gate. | D12 / R10 |
| `/v1/surveys/:id/responses` | POST | Emits exactly one cx event per response (`cx.nps_response` / `cx.csat_response` / `cx.ces_response` / `cx.survey_completed` based on survey type — unchanged behavior). No new event added (per D50 OQ5 resolution; supersedes earlier R22 plan to add `survey_completion`). | R22 |
| `/v1/public/surveys/:id/respond` | POST | Same emission as authenticated path. | R22 |

The new endpoints follow existing platform conventions (brandId-scoped, JWT auth, audit-logged); no new auth substrate.

## Compliance Requirements

Per `fraim/config.json customizations.compliance.regulations`: GDPR (in-scope), CCPA (in-scope), SOC2 (target month-12), PCI-DSS (minimal-scope — no card data here).

| Regulation | Clause | Mapped Control |
|---|---|---|
| **GDPR** | [Art. 7 §1 — controller must demonstrate consent](https://gdpr-info.eu/art-7-gdpr/) | The Consent collection sub-block (§2.1.1) inherits from brand by default. Per-survey override to a more-permissive mode fires the attestation modal which captures identity + timestamp + reason on every deviation. Audit-trail badge surfaces this on the detail page header. The attestation PATCH endpoint (absorbed from #283) writes `Survey.consentMode`, `Survey.consentSuppressedAttestedBy`, `Survey.consentSuppressedAttestedAt`, `Survey.consentReason`. |
| **GDPR** | [Recital 32 — consent must be unambiguous and informed](https://gdpr-info.eu/recitals/no-32/) | When the disclosure text is set (inherit from brand or per-survey override), it is shown to the respondent. The disclosure may be intentionally blanked for surveys whose respondents are outside GDPR/CCPA scope (e.g., respondents in non-covered US states, B2B internal employees) — in that case the form renders no consent block. The survey owner bears the legal authorization onus for that decision; the audit log records every disclosure-text and consent-mode change with actor + timestamp + new value, giving regulators and internal reviewers a complete traceability trail. The Look & Feel chrome matrix can hide brand logo / brand name / survey title but is independent of the consent block. |
| **GDPR** | [Art. 5 §1(b) — purpose limitation](https://gdpr-info.eu/art-5-gdpr/) | Disclosure text inherits `Brand.consentTextDefault`; per-survey override stored in `Survey.consentTextOverride`. The editor's `{{privacy}}` and `{{terms}}` tokens link to the brand's published policies, ensuring the respondent has access to purpose details at the moment of submission. |
| **CCPA** | [§1798.135 — right to opt out + Do Not Sell](https://oag.ca.gov/privacy/ccpa) | CustomerEQ does not sell PII. The audit trail produced by per-survey consent overrides supports `§1798.110` "right to know" requests by attributing every deviation to a named operator with reason text. |
| **CCPA** | §1798.105 — right to deletion | Out of scope here. The erasure-job + Art. 15 data-export endpoint are tracked under **[#264](https://github.com/mathursrus/CustomerEQ/issues/264)** (P1 — *Build GDPR erasure job + Art. 15 data-export endpoint per architecture.md §10 commitment*); not yet implemented but in the roadmap. The new consent-override columns introduced here are operational metadata (mode / attestation / reason text), not PII; they don't expand the erasure surface. When #264 lands, this row updates to reference the implemented job. |
| **SOC2** | CC6.1 — logical access controls | The PATCH endpoint that writes `Survey.consentMode` is gated by the existing brandId-scoped authorization check (project rule R6). The editor cannot write to a survey outside the operator's brand. |
| **SOC2** | CC7.2 — change monitoring | Editor tab saves route through the existing audit plugin (`apps/api/src/plugins/audit.ts`); `inferAction → survey.update` covers section saves. Consent-override writes specifically capture `metadata.consentMode` and `metadata.consentReason` per #283's binding decisions. |

## Validation Plan

| Layer | What | How |
|---|---|---|
| **Browser E2E (Playwright)** | Owner can create a draft, edit every tab, activate, and reach the Detail page; can override consent to IMPLIED with reason; can change theme + per-channel chrome and see preview update; can stop and restart. | New e2e at `apps/web/test/e2e/survey-admin.spec.ts`. Scenarios: (a) NPS preset → activate → respond, (b) Custom blank-canvas → embed-via-widget → respond, (c) consent-override happy path with attestation, (d) detail page (no responses → first response submitted → Distribution auto-collapses on revisit). |
| **Browser regression** | The list page never produces duplicate drafts under back/forward navigation. | E2E asserts the survey row count after the create → edit → back → forward → edit pattern. |
| **Browser regression** | Activate is gated on `Survey.questions.length >= 1`. | E2E asserts the disabled state of Activate when the question list is empty, and the enabled state after one question is saved. |
| **API integration** | All editor tab saves go through the same single PATCH endpoint shape, brand-scoped. | Vitest in `apps/api/test/integration/surveys-admin.test.ts`. Covers each tab's PATCH path and the unauthorized-cross-brand 403. |
| **API integration** | The PATCH `/v1/surveys/:id/consent-mode` endpoint (absorbed from #283) honors the attestation gate end-to-end. | Vitest in `apps/api/test/integration/surveys-consent-override.test.ts`. Includes: (a) override-to-more-permissive without attestation → 422; (b) with attestation → 200, audit row written; (c) override-to-stricter without attestation → 200; (d) override clear (set to null) → 200, audit columns cleared. |
| **Migration** | Schema rename `SurveyStatus.CLOSED → STOPPED` updates every existing row; UI references update lockstep. | Direct psql replay; assert `SELECT count(*) FROM "Survey" WHERE status='CLOSED'` is 0 post-migration; grep the codebase for `'CLOSED'` to confirm no stragglers in UI / API. |
| **Migration** | `Survey.incentivePoints` column dropped. Dead `EarningRule(triggerEvent='survey_completion')` rows fanned out to one rule per cx event type the program's surveys use; original rows deleted. For brands with `Survey.incentivePoints > 0`, auto-create per-type EarningRule per D50. Affected brand-count surfaced via Organization Settings notice. | Direct psql replay; assert: (a) `Survey.incentivePoints` column gone post-migration, (b) zero `EarningRule.triggerEvent = 'survey_completion'` rows remain, (c) every brand with prior `survey_completion` intent has matching live EarningRule rows with `triggerEvent` in {`cx.nps_response`, `cx.csat_response`, `cx.ces_response`, `cx.survey_completed`}, (d) idempotent on second replay. Per-brand fixture set: brand-with-only-Survey-value, brand-with-only-dead-EarningRule, brand-with-both, brand-with-live-cx-EarningRule, brand-with-neither. |
| **Worker pipeline** | The empirically-confirmed #225 bug (zero LoyaltyEvent rows after survey completion despite EarningRule existing) is fixed. | Vitest integration in `apps/worker/test/integration/survey-completion-earn.test.ts`: seed a program with `EarningRule(triggerEvent='cx.nps_response', pointsAwarded=50)`; seed a member enrolled in the program; submit a response to an NPS survey under that program; assert (a) a LoyaltyEvent row exists with `eventType='cx.nps_response'` and `pointsEarned=50`, (b) the member's `pointsBalance` advanced by 50, (c) atomicity preserved under transaction rollback. **Per-survey-type variant**: seed two rules (`cx.nps_response: 50`, `cx.csat_response: 20`); submit one of each; assert two LoyaltyEvent rows with the matching points. |
| **Migration** | New `Survey.title` column added as nullable; existing rows backfilled to `Survey.name` value so respondents see something during the transition. | Direct psql replay; assert post-migration `SELECT count(*) FROM "Survey" WHERE title IS NULL` is 0. |
| **Compliance** | Re-running the empirical reproduction from #225's `LoyaltyEvent`-zero bug yields a non-zero `LoyaltyEvent` after response submit. | Per the issue body's acceptance criterion. Recorded as a smoke test in staging post-deploy. |
| **Performance (NFR-P1)** | Editor auto-save PATCH p95 < 200ms / p99 < 500ms under typical load. | Bench: 100 sequential field-saves against a seeded survey; assert percentile bounds in a Vitest integration test. |
| **Performance (NFR-P3)** | Embed widget mount + first paint < 1s after host-page DOMContentLoaded. | Playwright on a sample host-page test fixture; assert `performance.measure('widget-mount')` duration. |
| **Accessibility (NFR-A1–A5)** | Editor + respondent form pass WCAG 2.1 AA. | Automated: `@axe-core/playwright` baked into the E2E suite; assert zero AA violations on editor + respondent surfaces. Manual: keyboard-only walkthrough of the editor tab flow + form submission. |
| **Security (NFR-S3)** | Disclosure-text override does not permit raw HTML. | Vitest unit: feed disclosure-renderer a payload containing `<script>` / `<img onerror>` strings; assert output is escaped and only the `{{privacy:"…"}}` / `{{terms:"…"}}` tokens resolve to `<a>` tags. |
| **Reliability (NFR-R2)** | Response submit is idempotent — same `idempotencyKey` doesn't double-credit. | Vitest in `apps/worker/test/integration/survey-completion-earn.test.ts`. Submit the same payload twice with identical `idempotencyKey = survey:${surveyId}:${memberId}`; assert exactly one `LoyaltyEvent` row exists and `Member.pointsBalance` advanced exactly once. |
| **Browser matrix (NFR-B2)** | Respondent-side form works on iOS Safari + Android Chrome (latest 2 each). | Existing E2E browser matrix extended to mobile browsers via Playwright projects config. |

## Alternatives

| Alternative | Why discard? |
|---|---|
| **Patch the existing three entry points in place** (add Edit buttons, fix duplicate-draft bug, rename "Launch", remove broken action types from picker). | Treats symptoms not the root cause. The 2026-05-10 directive is explicit about a green-slate redesign. Field-by-field patches accumulate as new bugs over time. |
| **Linear wizard for create, sectioned editor for subsequent edits.** | Wizard is what produces the duplicate-draft bug. Horizontal tabs handle both create and edit cleanly with Next/Back navigation; auto-save eliminates the duplicate-draft surface. |
| **Two parallel forms — an "easy" form and a "power" builder.** | Restates today's problem. Horizontal tabs + auto-save give "easy by default, power available" in one place. |
| **Keep both earning paths; add a mode switch.** | Adds an axis. The empirical bug is from having two paths at all. |
| **Keep `Survey.incentivePoints` as canonical** (the original direction in the issue body, before R5). | Mixes the loyalty-configuration layer (programs own earning) with the survey-configuration layer. Per the RBAC split (program admin / survey owner), the survey owner shouldn't be setting earning rates. Forces every `Survey.incentivePoints` change to fire an EarningRule sync — the original two-parallel-paths problem in a new shape. Reversed in D40 after R5 review. |
| **Show points on the survey form to boost response rate** (a `showIncentivePoints` toggle). | Singer & Ye (2013) finds in-form-visible incentives boost response *rate* but depress response *quality* (the user's concern: "users may trick by only clicking required buttons with arbitrary values"). Points-as-thank-you in invitation + post-submit is the right framing. |
| **Distribution-mode picker at draft time** (Standalone / Embedded). | Owner shouldn't have to pick. The same survey is reusable across every channel; the form-renderer adapts; per-channel chrome is set once in Look & Feel. |
| **Ship Rules / Post-Survey Actions in #241 V0.** | Rules / Post-Survey Actions are a separate iteration with their own sub-issues (#234 send_message, #242 award_reward, #246 wheel/scratch/mystery) each shipping end-to-end. Bundling them into #241 would make the umbrella PR unreviewable and tie a stable editor surface to in-flux action contracts. |
| **`Manage themes →` link in the survey editor's theme picker.** | The survey creator may not have theme-edit RBAC. The picker is a *selector*; theme creation lives in Organization Settings. |
| **Two state-page templates for the detail page** (DRAFT/no-responses vs. responses-exist). | A single shell with collapsible sections gives the same JTBD differentiation (Distribution expanded by default when empty; Configuration collapsed by default; Response placeholder regardless) without the cost of maintaining two layouts. |
| **Type as a meta pill under the survey name in the list.** | Type as a discrete column makes it scannable across rows and prepares for column-header sortability later. |

## Out of Scope (deferred)

| Item | Where it lives |
|---|---|
| **Sub-issue UIs for individual Post-Survey Actions** (`send_message`, `award_reward`, wheel/scratch/mystery, future `award_bonus_points`). | #234, #242, #246 — each ships end-to-end (UI + worker executor + delivery + tests) in a future iteration on Post-Survey Actions. The Rules tab is intentionally absent from this Epic's V0 so the editor surface ships stable. |
| **Response analytics surface on the detail page** (score distribution, sentiment, topic clusters, individual response stream, LoopMonitor). | Future sub-issue (sibling to #235). The detail-page Response section is a placeholder in V0. |
| **Per-survey override of the program's base earn rate** (e.g., "this specific survey awards 100 instead of the program's 50"). | V1 — likely a nullable `Survey.pointsOverride` column, resolved at runtime as `survey.pointsOverride ?? earningRule.pointsAwarded`. Editor would show an override input next to the inherited program value. Deferred per D40; filed when demand justifies it. |
| **Time-bounded earn rate boosts** ("double points this month for survey X"). | V1; the Campaign primitive already supports response-window-bounded boosts that layer on top of the EarningRule's base rate. New issue when demand surfaces. |
| **Cross-survey bulk operations** ("move all surveys in program X to a new program"). | Future; surveys are configured one at a time in V0. |
| **Audit-log dashboard for consent-mode deviations across surveys.** | Observability ask; separate issue when the audit-plugin events accumulate enough to warrant a UI. |
| **Survey response detail page** (the per-response viewer). | #235 — different surface, separate issue. |
| **Historical response import.** | #262 — orthogonal. |
| **Brand-level survey defaults** (e.g., "all surveys in this program default to NPS"). | Future; lives in #277 if demand justifies it. |
| **Native survey triggering** (automatic send on loyalty / CX events). | Future; type-picker rationale text in §2.1 acknowledges the use case but the trigger machinery itself is not part of #241. |
| **In-form points badge / incentive callout above questions.** | Out per §2.4. Not a future item — explicitly the wrong design call. |
| **Brand-issued opaque distribution refs** (per-invitation tracking — e.g., "this distribution batch had 14% response rate, that one had 38%"). Originally considered as Option B during OQ4 evaluation. | V0 ships with JS / data-attribute prefill (D51 / R16) which doesn't surface per-invitation analytics. Future enhancement: `POST /v1/distributions` endpoint mints opaque refs bound to `{ surveyId, externalRef }`; CustomerEQ stores token→externalRef mapping; URL has `?ref=<opaque>`; response analytics group by ref. Filed when invitation-tracking analytics demand surfaces. |
| **Encrypted URL-token integration mode** (asymmetric or symmetric key-pair relationship per brand; brand encrypts identifier client-side or server-side; CustomerEQ decrypts → `resolveOrEnrollMember`). Originally considered alongside Option B during OQ4 evaluation. | V0 uses out-of-URL prefill (R16 A1/A2). Future enhancement: for brands whose stack precludes JS prefill (e.g., third-party embed contexts with no JS execution rights, server-to-server pre-renders into static HTML emails), an encrypted-URL-token mode would let them keep URL-param ergonomics while keeping the identifier opaque to analytics / referrers / proxies. Key rotation, per-brand secret distribution, JWE-vs-HMAC choice are RFC topics when this is built. **Note**: this is structurally weaker than A (opaque blob is still in the URL, which some compliance regimes flag regardless of decodability); list as a fallback for constrained integrations, not the default. |
| **Theme creation / editing from inside the survey editor.** | Lives in Organization Settings; the survey editor's theme picker is a selector only, by RBAC design. |

## Competitive Analysis

This Epic is **internal UX coherence**, not a customer-visible differentiator. Per project rule R3 (Feature Parity Trap), the analysis avoids fabricating competitor specifics that would only exist to fill a checklist; the columns below are populated from publicly verifiable product docs and marketing collateral.

### Configured Competitors Analysis

*Analysis of competitors from `fraim/config.json customizations.competitors`.*

> `fraim/config.json` does not currently declare a `customizations.competitors` list. The right place to seed that list is the brand-creation / business-validation job, not a per-feature spec. Recommend deferring to a dedicated `project-onboarding` pass and treating the table below as the supplementary research for this Epic.

| Competitor | Current Solution | Strengths | Weaknesses | Customer Feedback | Market Position |
|---|---|---|---|---|---|
| *(not configured)* | — | — | — | — | — |

### Additional Competitors Analysis

*Analysis of survey-platform competitors and loyalty-platform competitors with overlapping CX surfaces, discovered during research for this Epic.*

| Competitor | Current Solution | Strengths | Weaknesses | Customer Feedback | Market Position |
|---|---|---|---|---|---|
| **Typeform** | Single editor + builder with live preview pane; per-form GDPR consent as a question type. | Best-in-class form aesthetics; same form ID renders across embed + share link + email; large template library. | No brand-level consent default to override; no loyalty-program integration; per-form pricing scales unfavorably for high-volume CX use cases. | G2 4.5 / 5 (~600 reviews) — most-cited praise: "beautiful forms"; most-cited gripe: pricing and limited logic. | Strong in marketing-form / lead-capture; moderate adoption in CX measurement; minimal share in loyalty operator stack. |
| **SurveyMonkey** | Sectioned editor (Design / Logic / Options / Collect); multiple "Collector" types per form. | Mature feature set; many distribution Collectors out of the box; per-survey consent text. | No brand-level defaults — every survey is configured individually; no loyalty integration; survey-builder UX feels dated. | G2 4.4 / 5 (~9,000 reviews) — praised for breadth; criticized for slow product evolution and pricing friction. | Largest installed base in general-purpose surveys; not specialized for CX-loyalty workflows. |
| **Qualtrics XM** | Single editor; library-level "Informed Consent" element overrides per survey. | Library inheritance is the closest pattern to CustomerEQ's brand-default + per-survey override; deep enterprise analytics. | Heavy implementation effort; requires CX research expertise to navigate; no native loyalty-program awareness. | G2 4.4 / 5 (~530 reviews) — praised for analytics; criticized for complexity and learning curve. | Enterprise CX leader; not designed for mid-market loyalty operators. |
| **Medallia** | Heavily consent-gated by design; account-level policy with per-survey exception flow gated by internal review queue. | Strong enterprise journey mapping; CX event integration. | Per-survey exceptions require workflow approval, not just admin attestation — heavy process; no native loyalty integration. | G2 4.4 / 5 (~280 reviews) — praised for enterprise-grade governance; criticized for setup time and customization cost. | Enterprise CX leader; not in mid-market loyalty operator stack. |
| **Annex Cloud** | Loyalty-program platform with embedded surveys; checklist-style configuration; no sectioned editor surface. | Native to loyalty program; survey participation can trigger reward earn. | Account-wide consent only — no per-survey override; survey-builder is minimal (no skip logic, limited question types); embedded is primary, standalone is afterthought. | Limited public review presence (sub-50 G2 reviews); mid-market loyalty operators report adequate survey functionality but mismatched UX between loyalty and CX. | Mid-market loyalty competitor with overlapping CX surface; direct CustomerEQ comparison. |
| **Delighted** (Qualtrics) | Trigger-based NPS only; minimal admin surface. | Lowest setup friction in the category; clear NPS focus. | Single survey type (NPS); no CSAT / CES; no per-survey consent override; no loyalty-program awareness. | G2 4.7 / 5 (~250 reviews) — praised for simplicity; criticized for the limited scope when the team's needs grow. | NPS-only specialist; mid-market CX-startup default; not a loyalty player. |

### Competitive Positioning Strategy

#### Our Differentiation

- **Loyalty-moment-aware surveys with per-type earning configuration**. No competitor ties survey design + earning to the same loyalty record. CustomerEQ's editor reads from `Survey.programId` to show the points value resolved from the program's `EarningRule(triggerEvent='cx.<surveytype>_response')` — a programmatic link no survey tool offers, and one that naturally supports different earn rates per survey type within the same program. The same loop continues post-response: the cx event credits via the existing loyalty pipeline, with full `LoyaltyEvent` audit history.
- **Brand-default + survey-override with attestation, not approval workflow**. Qualtrics requires library element edits; Medallia requires internal approval queues. CustomerEQ's editor combines self-service per-survey override with single-step admin attestation — operator agility with audit-trail backstop. The attestation modal captures identity + reason + timestamp; the audit log is the receipt.
- **One survey, many channels, one renderer.** Most competitors treat each "Collector" (or embed / link / email) as a separate object. CustomerEQ surveys are channel-agnostic at the data layer; the channel-aware form-renderer applies the Look & Feel chrome matrix at render time. Survey owners don't duplicate work to publish across channels.
- **Unified editor surface, not "easy form" + "power builder".** Today's three-entry-point fragmentation is being closed; the V0 redesign delivers a single section-tabbed editor with auto-save. Wizards remain in competitor products that haven't reconciled the trade-off.

#### Competitive Response Strategy

- **If Typeform / SurveyMonkey push deeper into CX measurement** (likely via partnerships with loyalty vendors): CustomerEQ doubles down on the loyalty-aware features competitors can't replicate without a member graph (points-aware thank-you, per-program earning resolution, Loop Monitor on detail page). The differentiator deepens; the parity surface stays minimal.
- **If Qualtrics / Medallia push down-market with simpler editors**: CustomerEQ emphasizes the loyalty integration and mid-market price/effort point. The "operator agility over policy bureaucracy" framing remains the core message.
- **If Annex Cloud closes the CX-survey gap** (most direct threat): CustomerEQ accelerates Post-Survey Actions and AI-synthesis rollout (sub-issues #234 / #242 / #246 + the response-detail audit on #235). The end-to-end loop is the moat.

#### Market Positioning

- **Target segment**: mid-market loyalty operators who want CX measurement embedded in loyalty workflows, not as a separate tool. ICP per `docs/business-development/business-validation-report-cx-loyalty-platform-2026-03-24.md`.
- **Value proposition**: the platform that closes the loop from survey response → loyalty action → measurable program impact, in a single operator surface.
- **Pricing posture**: this Epic does not change pricing. The redesigned editor improves activation conversion + operator productivity; whether to bundle the consent-override / AI-synthesis features into premium tiers is a pricing-strategy decision deferred per `feedback/preferences` (pricing-not-finalized rule).

### Research Sources

- Typeform help center — "Embed your form" + "GDPR consent question" articles. Last verified 2026-05-10.
- SurveyMonkey help center — "Collector types" + "Privacy and consent settings". Last verified 2026-05-10.
- Qualtrics XM Library docs — public help center. Last verified 2026-05-10.
- Medallia public marketing collateral on CX governance and consent. Last verified 2026-05-10.
- Annex Cloud public marketing site — loyalty-program + survey integration pages. Last verified 2026-05-10.
- Delighted (Qualtrics) product page — feature scope + pricing. Last verified 2026-05-10.
- G2 review counts and ratings — public G2.com listings as of 2026-05-10.
- Singer & Ye (2013), "The Use and Effects of Incentives in Surveys" — meta-analysis on incentive transparency vs response quality. Cited under §2.4 and Alternatives.
- CustomerEQ codebase — every file path referenced in this spec was read during context-gathering.

> Caveat: competitor product docs and G2 listings are subject to change without notice. The shapes named above are current to the best of my knowledge as of the dates noted; if the implementation review surfaces a contradiction with current docs, prefer the contradiction and update.

## Cross-references

- **Parent**: #225 (broader CX-to-loyalty coherence).
- **Absorbs**: **#232** (earning consolidation; direction shifted to elimination per V0 + future Campaign-based override).
- **Absorbs**: **#283** (consent-override end-to-end) — the PATCH endpoint contract + audit-plugin extension + survey-editor consent panel + attestation modal + audit-trail badge all ship under #241's umbrella PR. #283 closes as merged-into-#241 once #241's implementation PR lands.
- **Sub-issues** (deferred to a future iteration on Post-Survey Actions): #234 `send_message`, #242 `award_reward`, #246 wheel/scratch/mystery.
- **Sibling**: #235 (response-detail page), #262 (import flow).
- **Adjacent**: #277 (Organization Settings — brand defaults the survey editor inherits), #276 (P0 hotfix; data unblock done, UX absorbed here), #292 (brand-level consent admin, recently shipped), #291 (BrandTheme / SurveyTheme split — the theme library this editor picks from), #117 (`responsePolicy` precursor), #231 (response data model — schema columns this spec renders), **#79 (trigger wizard — collapses to the type-card grid + guidance link in Basics)**.

---

## Appendix: Decision Log

Iteration history — every decision made during R0–R4 of the mock-and-spec convergence. Preserved for traceability; the main body above is authoritative.

| # | Round | Decision | Rationale |
|---|---|---|---|
| D1 | R0 | One unified surface — three entry points collapse into `/admin/surveys/[id]/edit`. | Three parallel entry points produced duplicate drafts and "where do I edit?" confusion. |
| D2 | R0 | Section-tabbed editor, not a linear wizard. | Surveys are non-linear; wizard pattern produces the duplicate-draft bug. *Refined by D25 to horizontal tabs.* |
| D3 | R0 | ~~Distribution mode (Standalone/Embedded) as a first-class draft-time field.~~ | **SUPERSEDED by D10.** |
| D4 | R0 | ~~Earning collapses to programs-only; the `Survey.incentivePoints` path is canonical.~~ | **SUPERSEDED by D40** — direction inverted: programs own earning configuration; surveys consume it. |
| D5 | R0 | Vocabulary: Save Draft / Activate / Stop / Restart / Discard. Schema rename `CLOSED → STOPPED`. | Issue body directive; consolidates inconsistent verbs. |
| D6 | R0 | ~~Rule-action picker shows only end-to-end-shipping types.~~ | **SUPERSEDED by D14.** |
| D7 | R0 | Consent panel inside the editor; override surfaces only the option that differs from brand default. | Per #276 round-1 reviewer note carried into #283. |
| D8 | R0 | Theme picker over BrandTheme library; per-survey thank-you copy + redirect in editor. | Per #291's split. *Refined by D18, D30, D36.* |
| D9 | R1 | Section-tabbed editor confirmed (Q1 answer). | User picked section-tabbed over linear wizard / hybrid overlay. *Refined by D25 to horizontal tabs.* |
| D10 | R1 | Distribution is multi-channel by default; no picker. The form-renderer adapts per channel. | User R1: "Distribution method is not a choice. The same survey can and should be usable across different mechanisms." |
| D11 | R1 | ~~Earning sweep = full delete (deletes `EarningRule(triggerEvent='survey_completion')` rows).~~ | **SUPERSEDED by D40** — the kept path is `EarningRule(triggerEvent='survey_completion')`; the dropped path is `Survey.incentivePoints`. |
| D12 | R1 | #283 absorbed into #241. | The consent panel UI + PATCH endpoint + audit extension are all part of the same unified editor surface. |
| D13 | R1 | ~~Rename `award_points` rule action to disambiguate.~~ | **OBVIATED by D14** — rules deferred entirely. |
| D14 | R1 | Rules / Post-Survey Actions deferred entirely from #241 V0. | User R1 POV: keep rule logic as Post-Survey Actions iterated under sub-issues. |
| D15 | R1 | Filename convention: `<issue#>-<feature-name>` (drops `-view` / `-epic` suffixes). | Aligns with `277-organization-settings.*`, `35-survey-builder.html`. |
| D16 | R1 | New nullable `Survey.title` column (respondent-facing); `Survey.name` stays admin-facing. | Admin names accumulate version context that shouldn't reach respondents. |
| D17 | R1 | Editor has 4 sections; Consent is a sub-block inside Basics. | User answer C2(a). |
| D18 | R1 | Theme picker lives in Look & Feel only; no Basics-level default. | User answer C3(a). *Refined by D29 (preview-first), D36 (no manage link).* |
| D19 | R1 | `Survey.showIncentivePoints` toggle removed entirely; points never appear on the form. | User confirmed C4 recommendation. Singer & Ye (2013): in-form-visible incentives depress response quality. |
| D20 | R1 | Look & Feel has per-channel chrome toggles (matrix) for Brand logo / Brand name / Survey title × Standalone / Embedded. | User R1: "each mode would allow any of brand logo, brand name, title to be hidden." |
| D21 | R1 | Type picker is a 4-card grid in Basics + a "Not sure?" guidance link (collapses #79's trigger wizard). | Type guidance is #79's actual deliverable once trigger framing is stripped. |
| D22 | R1 | List corrections: Type pill in row, Type filter chips, Trigger column dropped, View icon dropped, row click → Detail. | User R1 list feedback. *D26, D39 refined the layout further.* |
| D23 | R1 | ~~Detail page is JTBD-aware (State A / State B page templates).~~ | **SUPERSEDED by D32/D37.** |
| D24 | R1 | Activate gates on ≥1 question; modal shows share link + embed snippet. | User R1 directive. *Refined by D31 (modal simplified; share/embed move to detail page).* |
| D25 | R2 | Editor layout reverses to horizontal tabs (with Next/Back); the sticky vertical TOC goes. | User R2: "Questions section feels too cramped." With 4 tabs, horizontal is roomier. |
| D26 | R2 | Survey description surfaces on the list as a muted second-meta line under the name. | User R2 question on where description appears. |
| D27 | R2 | Response policy + Consent collection rendered as dropdowns, not stacked radio cards. | User R2: "drop downs to simply screen space." |
| D28 | R2 | Consent UX matches the #277 admin disclosure pattern (dropdown → preview-card → disclosure textarea). | User R2 directive. *D34 refined the disclosure toolbar specifically.* |
| D29 | R2 | Look & Feel section is preview-first; theme + chrome matrix sit below the preview. | User R2: "Should we show the preview first and then the customization?" |
| D30 | R2 | Theme picker shows 4 brand-provisioned defaults + any additional brand themes. | User R2: "We populate with 4 defaults." *Refined by D36 (no manage link).* |
| D31 | R2 | Activate modal simplifies — no share/embed inside; redirects to Detail page on confirm. | User R2: "Once Activate is clicked, the page can redirect to the Survey Detail page." |
| D32 | R2 | Detail page = 3 vertical sections (Distribution wide + collapsible, Response placeholder, Configuration summary). D23's two-state templates collapse into Section 1's expanded/collapsed default. | User R2 directive on detail-page restructure. |
| D33 | R2 | Mock is fully interactive (JS-wired). | User R2: "Make the mock completely interactive, so reviewers can actually see the flow." |
| D34 | R3 | Disclosure text override uses the same `.consent-toolbar` editor as Organization Settings — `[+] Privacy link`, `[+] Terms link`, `Reset to brand default`. Tokens render to brand URLs at render time. | User R3: explicit call-out that the disclosure UX should match the admin pattern. |
| D35 | R3 | Live Preview is channel-first: Standalone / Embedded as top tabs; Desktop + Mobile side-by-side under each. | User R3: channel is the meaningful semantic axis; viewport is the visual-fit axis. |
| D36 | R3 | Theme picker shows all brand themes; no `Manage themes →` link in the survey editor. | User R3 + RBAC: survey creator may not have theme-edit access. |
| D37 | R3 | All detail-page sections are collapsible; chevron icon converges with #277 (`▼` rotating -90deg when collapsed). | User R3: same icon set as Organization Settings. |
| D38 | R4 | "Not sure which to pick?" type-guidance toggle uses the same chevron pattern as the rest of the platform. | User R4: chevron consistency. |
| D39 | R4 | Type is a discrete column on the surveys list (not a meta pill under the name). Prepares for column-header sortability. | User R4: "Type deserves its own column to call attention." |
| D40 | R5 | **Earning consolidation direction reversed.** Programs own earning configuration; surveys consume it. The canonical path is `EarningRule(triggerEvent='survey_completion', pointsAwarded=N)` defined on the program. The `Survey.incentivePoints` path is the one that gets dropped, not kept. The Points & Thank You section's points value is **read-only**, sourced from the program's earning rule (plus active campaign multipliers at runtime). The empirically-confirmed prod bug from #225 (member submits survey → `pointsBalance` stays 0) is fixed by wiring the survey-response handler to emit `survey_completion` events consistently and ensuring the worker's EarningRule evaluator handles them — NOT by abandoning the EarningRule path. **V1 deferred**: per-survey override of the program's earning rate (likely a nullable `Survey.pointsOverride` column). | User R5: "Survey.incentivePoints is NOT the single source of truth. Earning consolidates to programs-only and earning rule has to be triggerEvent='survey_completion'. The points shown in the mock at Points and Thank you should come from the program & campaign point evaluation for survey completion. If it is easy, they can be made overwritable in Survey, but suggest deferring it to V1." Programs own earning; surveys consume earning. This matches the loyalty-domain RBAC split (brand admin / program admin configures earning rules; marketing manager configures surveys) and removes the design pressure on the survey owner to set earning rates. D4 and D11 followed the original #241 issue body's direction verbatim; user's R5 reversal supersedes both. | UX §2.4 + §4 + Migration + Alternatives |
| D41 | R6 (PR #314 review) | **Blank disclosure text = no consent block rendered** (not "R17 suppress-UI attestation mode"). The blank value is a valid setting for surveys whose respondents are outside GDPR/CCPA scope (e.g., respondents in Washington state, B2B internal employees). Survey creator bears the legal authorization onus; the audit log captures every disclosure-text and consent-mode change for traceability. The Preview card renders empty when disclosure is blank. | User R6 inline comment on `docs/feature-specs/241-survey-admin-ux.md:200`: "This is incorrect. The text is allowed to be blank in which case consent text does not appear. An audit log creates traceability. This option exists primarily for surveys focused in regions where GDPR or CCPA do not apply - e.g. surveys to Washington residents. Survey creator has the onus to ensure that they have the right legal authorization." Also user R6 comment on L87: "or empty when the text is empty". My prior framing ("empty string = R17 attestation mode (suppress disclosure UI)") imported #231 R17 vocabulary that doesn't fit this surface. The new framing is simpler: blank = blank, audit log is the traceability. | UX §2.1.1 + R13 / R14 + Compliance §GDPR Recital 32 |
| D42 | R6 (PR #314 review) | **Member-identification is channel-aware in V0**: Standalone surveys render a member-identification field on the form (using the brand's `memberIdentifierKind` from #277); Embedded surveys consume member ID from `?member_id=…` URL param at widget mount; if the URL param is missing, the embedded form falls back to the Standalone prompt. Look & Feel preview reflects each channel's flow. | User R6 inline comment on L103: "In the mock, I forgot to notice that in Standalone surveys a field needs to appear to capture the member ID based on the brand setting. In Embedded, we expect that to be passed in the URL in V0. This is the behavior we expect to preview. In actual execution if embedded doesn't receive a member ID, the form needs to prompt for it." | UX §2.3 + R15 / R16 |
| D43 | R6 (PR #314 review) | **Thank-you message V0 variables reduced** to `{{points}}`, `{{pointCurrencyName}}`, `{{rewardLink}}`. `{{programName}}` removed (internal concept not for respondent display). `{{memberName}}` removed (name is not guaranteed on the Member record — at best `memberId` exists, which isn't a respondent-friendly value). Default copy updated to V0 variables only. | User R6 inline comment on L112: "Program name is an internal concept and should not be exposed to members. {{Member Name}} may not be available at all on the survey, at best we have memberID. Suggest removing this for V0. Rest of the fields are correct. In the spec {{points}} is repeated twice." | UX §2.4 + R21 |
| D44 | R6 (PR #314 review) | **All survey state transitions emit audit log entries.** Every transition (Activate / Pause / Resume / Stop / Restart / Discard / Delete) writes `{ actorUserId, surveyId, fromStatus, toStatus, timestamp }` via the existing audit plugin. Feeds compliance reporting + any future "survey activity" view. | User R6 inline comment on L166: "All Survey state changes must be logged for audit and reporting purposes". Existing audit-plugin infrastructure already supports this shape (per #277 + #283); #241's implementation extends it to every survey-state-mutation route. | UX §5 + R24 |
| D45 | R6 (PR #314 review) | **Spec adopts FRAIM standard format** — Functional Requirements section in SHALL-style with Given/When/Then acceptance criteria + traceability tags `R1`–`R28` + Open Questions block. Competitive Analysis section restructured to match FRAIM template: Configured Competitors Analysis + Additional Competitors Analysis + Competitive Positioning Strategy (Our Differentiation / Competitive Response Strategy / Market Positioning) + Research Sources. | User R6 conversation comment: "Why are requirement not as SHALL statements as required by FRAIM? Competitor analysis also doesn't seem to match FRAIM's standard." The earlier rounds had behaviors only in narrative §1–§7 form, which works for design review but is not testable language. The SHALL section is now derivable from the UX prose and serves as the contract for tests + the RFC's traceability matrix. | Spec-wide structure |
| D46 | R6 (PR #314 review) | **CCPA right-to-deletion** is tracked under [#264](https://github.com/mathursrus/CustomerEQ/issues/264) (P1 — *Build GDPR erasure job + Art. 15 data-export endpoint*). The earlier spec text claimed "the existing `apps/worker` erasure job pattern" — that job does not exist yet. Updated to reference the in-roadmap issue. | User R6 inline comment on L203: "This job does not exist yet. Will need to implement, but is in the roadmap. Identify the issue and update". Followed memory pattern *"Asserted facts about file / config / external-state contents without reading the primary source first"* — I imported aspirational architecture language that didn't match the codebase. | Compliance §CCPA |
| D47 | R7 (post-RCA audit) | **Spec adds Non-Functional Requirements + Error States + Schema/API Summary sections.** Per user R7 audit request: "I don't see any Non Functional requirements in the spec. So do a thorough audit and correct as needed." Applied the freshly-defined preventive controls from the RCA (CTRL-2: FRAIM template compliance pass at submit; CTRL-3: primary-source verification before assertion; CTRL-4: per-section orthogonal-axis pass). New top-level NFR section with 9 categories (Performance / Security / Reliability / Scalability / Accessibility / Observability / Internationalization / Browser compatibility / Backward compatibility & migrations). New Error States subsection within Functional Requirements consolidating 12 edge cases scattered across G/W/T. New "Schema and API Summary" section that consolidates the scattered schema-delta and API-surface change descriptions for the RFC author's reference. Validation Plan extended with 6 new rows covering Performance / A11y / Security / Reliability / Browser-matrix NFR verification. | User R7: "Can you also check again the feature specification document and any new missing / incorrectly written sections? I don't see any Non Functional requirements in the spec. So do a thorough audit and correct as needed before I review again." This is the first application of the RCA preventive controls — proves the loop closes: RCA identifies the failure mode → preventive controls defined → next request triggers the controls → gap caught and filled before user review. Two new claims in the NFR section were verified against primary sources before writing (CTRL-3): `apps/api/src/plugins/audit.ts` exists (5607 bytes verified); `apps/api/src/routes/surveys.ts` contains 36 brandId references and PATCH endpoints at lines 126 / 160 verified; `apps/web/package.json` confirmed to have no i18n library (next-intl / react-intl / i18next absent). **Note**: this audit was incomplete — controls were applied forward-only to new content. Meta-RCA in R8 (D48) captures the retrospective sweep that should have happened. | Functional Requirements (Error States) + new NFR section + new Schema/API Summary section + Validation Plan amendments |
| D48 | R8 (meta-RCA — retrospective sweep) | **Apply R6 RCA's controls retrospectively across the entire existing spec**, not just forward to new content. User R8 ask: "do an RCA for why you messed up even after I asked you to check for some misses I identified and do an RCA". The R7 audit had applied controls only to new sections (NFRs / Error States / Schema-API), leaving 9 concrete gaps in the existing spec: (a) **Epic ACs not mapped** — AC-5 (rule-action picker) and AC-6 (Survey.incentivePoints contradicted by D40) needed explicit traceability; **fixed**: added "Epic Acceptance Criteria — traceability map" table near top of Functional Requirements. (b) **Internal contradiction** — §4 said "applies any active campaign multipliers" while OQ3 said this is unresolved; **fixed**: softened §4 to defer to OQ3; R20 same. (c) **Unverified assertion** — NFR-BC1 cited `scripts/check-migration-idempotency.sh` which doesn't exist (same shape as the erasure-job miss in R6); **fixed**: replaced with verified reference to `.github/workflows/ci.yml:75` (the actual migration-idempotency gate). (d) **Orthogonal axes missed** — R20 (program-rate change after activation), R8 (anonymous responses + responsePolicy), Schema deltas (neither-field case), NFR-R5 (pipeline resilience under worker downtime / queue backpressure); **fixed**: all four added with G/W/T criteria. (e) **Ambiguous statement** — NFR-S5 "IP (if available)"; **fixed**: definitive `requestIp` from `request.ip` with structured-log warning fallback. (f) **Premature convergence claim** — spec status was "Converged after 7 review rounds"; **fixed**: status now "Ready for review (round 8 — RCA controls applied retrospectively; awaiting reviewer signoff)". Mock parity gaps (live char counter, save-failed indicator, embedded fallback prompt, blank-disclosure audit badge) will be addressed in the same commit. **Four new controls** filed in raw learnings for sleep-on-learnings synthesis: CTRL-5 (retrospective sweep at audit requests), CTRL-6 (Epic-AC traceability map), CTRL-7 (mock-vs-spec parity sweep at spec-only rounds), CTRL-2 amendment (no unilateral convergence claim). | User R8: requested meta-RCA because R7's audit was incomplete despite the R6 RCA. Root meta-cause: R6 controls were specified as forward-looking only ("before writing the sentence") so they didn't drive retrospective sweep. Existing L1 memory "Audit mock-vs-spec at every spec round" at score 8.0 also didn't fire — same memory-channel-reliability problem the R6 RCA had already flagged. | Functional Requirements (new AC traceability table; R8 anonymous responses axis; R20 program-rate-change axis) + Schema deltas (neither-field row) + NFRs (R5 pipeline resilience; S5 IP definitive; BC1 real CI gate) + Status field + Decision Log footer |
| D49 | R9 (PR #314 review batch on R8 commit) | **13 reviewer inline comments on the R8 spec; addressed in this commit.** Concrete updates: (a) **R1 amended** — old entry-point routes deleted (not redirected) per reviewer "Should these survey paths even exist, or can they be eliminated?"; 404s on direct navigation. (b) **R6 extended** with type-change-after-questions-populated behavior — confirmation modal on type-change; Custom skips the modal. (c) **R8 extended** with explicit ONCE-second-submit G/W/T returning HTTP 409 + respondent-facing copy. (d) **R26-R28 + new R32** detail-page collapse defaults restructured per reviewer's JTBD framing: Distribution + Configuration summary expanded when `responsesCount === 0`, collapsed when `>0`; Response collapsed when `responsesCount === 0`, expanded when `>0` (inverse of the other two). (e) **R29 added** — editability after Activate: only response-data-non-invalidating fields editable in ACTIVE/PAUSED; HTTP 409 on disallowed edits; STOPPED fully non-editable. (f) **R30 added** — Basics-tab response-policy field becomes read-only once `responsesCount > 0` independent of state. (g) **R31 added** — theme-token → element mapping (primaryColor / backgroundColor / textColor / headingFont / bodyFont / borderRadius → specific form elements). (h) **OQ1, OQ2, OQ3 RESOLVED** with reviewer answers captured verbatim. (i) **OQ4 OPEN (NEW)** — security: R16's `?member_id=` URL surface has same PII-in-URL pattern as `#209`; recommended resolution: absorb #209's opaque-token approach. Verified the email-in-URL at `apps/web/src/app/survey/[id]/page.tsx:254` and `apps/api/src/routes/public.ts:402`. R16 amended to flag the upcoming change. (j) **OQ5 OPEN (NEW)** — reviewer asked "what is the existing survey event and why should we have two?"; documented the two-event design (CX events for SurveyRule/Campaign type-aware matching; new `survey_completion` for type-agnostic EarningRule), recommended deferring consolidation to a separate event-vocabulary sub-issue. | User R9 review on PR #314 — 13 inline comments delivered in one batch on 2026-05-11. All addressed; per-thread replies posted with this commit's SHA. Mock follow-up needed: ACTIVE-state read-only field rendering for R29/R30 (deferred to next round with explicit callout per CTRL-7). | UX §1 (R1 amended), §2.1 (R6 / R8 amended), §2.3 (R31 added), §7 + Detail page R26-R32, OQ1-OQ5 |

| D50 | R10 (PR #314 conv-level pushback on OQ5) | **OQ5 resolved: single-event design, type-aware EarningRule trigger.** Reviewer rejected the two-event design (defer-consolidation) I had recommended in D49. Their argument: survey creation has one entry point (this Epic), so the survey-response handler is the only producer; Survey already knows its type; emit the type-specific cx event and let `EarningRule.triggerEvent` match it directly. No new `survey_completion` event. **Side benefit**: program admins naturally get per-survey-type earning configuration — one program can award different points for each survey type via one EarningRule per cx event. **Verified against primary source** (CTRL-3): `apps/worker/src/processors/loyaltyEvents.ts:43-105` already does exact string match on `rule.triggerEvent === eventType`; no worker code change needed. **Cascade**: R20 + R22 amended; §4 (Single earning path) rewritten; NFR-R2 simplified (one idempotency key, not two); NFR-R5 + NFR-SC4 updated; Migration plan rewritten with fan-out of dead `survey_completion` rules; Schema delta table updated; API surface row simplified; Validation Plan migration + worker-pipeline rows updated; Competitive Analysis bullet updated; #315 marked closeable under this design (no `survey_completion` event to wire). | User R10 conversation comment (after PR #314 thread `3216773456`): "Survey creation has one entry point — this issue. Where else do we expect the other cx events to be raised from? Survey knows the type of survey it is: nps survey, csat survey, ces survey or custom survey. Shouldn't it emit the corresponding event based on survey type completed. This can then be tied to program creation giving admin the flexibility to award different points for each type of survey. I don't see the point of creating a new survey_completion event." My R9 recommendation (defer consolidation, emit two events) was wrong — same pattern as D40 (premature acceptance of issue-body-derived direction without surfacing as OD). The user's design is cleaner: simpler (one event), more expressive (per-type points), less code churn (no new emission), aligned with existing worker contract. | UX §2.4 R20, §4 entire section, NFR-R2/R5/SC4, Migration plan, Schema delta table, API surface table, Validation Plan, Competitive Analysis, OQ5 resolution, #315 closeable |

| D51 | R11 (OQ4 resolution) | **OQ4 RESOLVED: JS / data-attribute prefill, no URL identifier — absorbs #209's privacy fix.** Reviewer rejected R10's opaque-token recommendation because it breaks #231's auto-enroll path (no token to issue for first-time visitors). Five alternatives evaluated in chat; reviewer picked JS prefill. **The reframe**: PII reaches the server via POST body (`resolveOrEnrollMember` at `apps/api/src/routes/public.ts:295` consumes `memberId / email / phone / firstName / lastName` from POST fields) — verified primary source. URL is purely a pre-fill convenience; removing it doesn't break auto-enroll. **Two patterns supported**: (A1) `data-prefill-*` attributes on the embed script tag (server-rendered brands; one-paste ergonomic preserved); (A2) `CustomerEQ.surveys.prefill(surveyId, { ... })` JS call (SPA brands). Brand picks per their stack. **Effects in this commit**: R16 rewritten with both patterns + Given/When/Then for each; §2.3 narrative updated; NFR-S4 updated (member identity never via URL); Error States row updated (legacy URL-param surveys ignore the param + fall back to R15 prompt). **Future enhancements** added to Out of Scope: Option B (brand-issued distribution refs for invitation analytics) + encrypted-URL-token mode (asymmetric/symmetric per-brand keys; fallback for brands whose stack precludes JS prefill). Both are deferred; neither is V0. | User R11 conversation feedback rejecting the token-resolved-server-side approach because "CustomerEQ doesn't have the complete member list and auto-enrolls members based on responses #231. So if CustomerEQ receives a token, how will it get ID?" Followed-up by picking JS prefill (Alternative A) after reviewing the five-option matrix in chat. Confirmation: "Leaning towards A... OK". | UX §2.3 + R16 + NFR-S4 + Error States + Out of Scope (2 new rows) |

🤖 Iteration history: R0–R11. R11 closes OQ4 with the JS-prefill design (D51) and lists two future enhancements (brand-issued distribution refs + encrypted-URL-token mode). **Convergence is the reviewer's call** — this spec awaits reviewer signoff on PR #314 before being declared converged.
