# Issue #177 — Implementation Work List

**Issue**: Upgrade Node 20 → 22 in Dockerfiles (api, web, worker)
**RFC**: `docs/rfcs/177-upgrade-node-22.md`
**Branch**: `feature/177-upgrade-node-20-22-in-dockerfiles-api-web-worker`
**PR**: #188 (design phase already approved; implementation lands on the same PR)

## Issue Type
**Feature** — runtime LTS bump. Not a bug; no repro phase needed.

## Scope Summary

8 file edits (~10 lines) + 1 new CI job. Total touch is well under the 15-file phase-split threshold. Single-PR deliverable.

## Validation Requirements

- `uiValidationRequired`: **No** — no UI changes; runtime swap only. `/healthz` payload unchanged.
- `mobileValidationRequired`: **No** — no UI surface.
- Browser/responsive: **N/A**.
- Build validation: **Required** — `pnpm build`, `pnpm typecheck`, `pnpm lint`, `pnpm test`, plus a fresh local `docker build` of all three Dockerfiles against `node:22-slim`.
- Integration validation: **Existing** — `pnpm test:integration` against the local Docker Postgres + Redis. Must continue to pass.
- E2E validation: **Out of scope** — RFC explicitly defers e2e-in-CI; pre-existing gap.
- Native binding validation: **Required** — confirm `pnpm db:generate` succeeds inside the Node 22 builder stage of all three Dockerfiles (Prisma engine binary download).

## Implementation Checklist

### Code changes (8 surfaces)

- [ ] `Dockerfile.api` line 2 — `FROM node:20-slim AS builder` → `FROM node:22-slim AS builder`
- [ ] `Dockerfile.api` line 29 — `FROM node:20-slim AS runner` → `FROM node:22-slim AS runner`
- [ ] `Dockerfile.web` line 2 — `FROM node:20-slim AS builder` → `FROM node:22-slim AS builder`
- [ ] `Dockerfile.web` line 37 — `FROM node:20-slim AS runner` → `FROM node:22-slim AS runner`
- [ ] `Dockerfile.worker` line 2 — `FROM node:20-slim AS builder` → `FROM node:22-slim AS builder`
- [ ] `Dockerfile.worker` line 29 — `FROM node:20-slim AS runner` → `FROM node:22-slim AS runner`
- [ ] `.github/workflows/ci.yml` line 49 — `node-version: '20'` → `node-version: '22'`
- [ ] `package.json` line 7 — `"node": ">=20.0.0"` → `"node": ">=22.0.0"`
- [ ] `package.json` line 36 — `"@types/node": "^20.0.0"` → `"@types/node": "^22.0.0"`
- [ ] `pnpm-lock.yaml` — regenerated by `pnpm install` after the package.json change. Diff must be limited to `@types/node` and any peer-dep adjustments. No `pnpm update`.

### CI job addition (decision 1: bundled in this PR)

- [ ] `.github/workflows/ci.yml` — add a new `docker-build` job that builds all three Dockerfiles after the `ci` job succeeds. Web build passes dummy `NEXT_PUBLIC_*` build-args (build-only validation, not runtime).

### Documentation updates (decisions 2 & 3)

- [ ] `docs/architecture/architecture.md` line 28 — `Node.js >= 20` → `Node.js >= 22`, plus add the LTS-tracking sentence: "Track upstream Node LTS — bump within ~6 months of EOL."
- [ ] `docs/getting-started.md` line 11 — `Node.js | >= 20 | nvm install 20` → `Node.js | >= 22 | nvm install 22`

## Quality Requirements

- Constitution compliance: Integrity (only claim a test passes if I ran it and saw the output); Correctness (lint/typecheck/test all green); Simplicity (only the 8 surfaces + 1 CI job — no incidental cleanups).
- Testing standards: Run the full validation suite (`pnpm build && pnpm typecheck && pnpm lint && pnpm test && pnpm test:integration`) and capture output. Run `docker build` for all three Dockerfiles locally and capture output.
- Architecture standards: No layer changes; runtime bump only. Documentation update lands in the same commit as the runtime bump (rule #5).

## Patterns Discovered (from design phase)

- **Branch naming**: FRAIM `prep-issue.sh` produces `feature/{N}-{slug}` (already on this branch). Project rule #10 says `feature/issue-{N}-{slug}` — known drift; FRAIM-driven naming wins for this issue per the user's "Ensure you use FRAIM" directive.
- **Engines vs Dockerfile coupling**: Both move together (RFC's "Customer Problem" section establishes this).
- **Architecture doc edits land in implementation PR**: Per FRAIM convention captured in the 157 postmortem — design phase proposes; implementation phase applies.
- **`docs/evidence/28-deploy-evidence.md:58` is immutable history**: The `node:20-alpine` reference there is not edited.

## Known Deferrals (locked in by RFC)

- Adding Playwright e2e to CI — separate issue.
- Real-Redis integration tests — separate issue.
- Image-boot smoke (`docker run` + `curl /healthz` in CI) — separate issue.
- Bumping pnpm 9 → 10, Prisma/Fastify/Next/BullMQ — separate.
- Switching to `node:22-alpine` — deliberately staying on Debian slim per OpenSSL 3 episode.
- Build-time Node-version templating (single source of truth across the 8 surfaces) — flagged in RFC as out-of-scope; accept N=8 duplication and rely on the audit checklist.

## Open Questions

None. All three reviewer decisions confirmed:
1. **Bundle** the new `docker-build` CI job into this PR ✅
2. Take the proposed `architecture.md` LTS-cadence sentence ✅
3. Bump engines floor from `>=20.0.0` to `>=22.0.0` ✅
